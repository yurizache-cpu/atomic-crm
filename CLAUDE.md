@AGENTS.md

# AI Company OS

Read this before doing anything. The repository is the project memory; conversation history is not.

| | |
| --- | --- |
| **What this is** | An AI Company OS being built **on top of** a fork of [Atomic CRM](https://github.com/marmelab/atomic-crm). Companies, departments, AI employees, workflows, events, tasks, decisions, reviews, approvals, permissions, risk policy, cost accounting and audit trails as first-class data. |
| **First tenant** | An online psychology clinic (Brazil, LGPD). A later tenant may be a 3D-printing business. |
| **Current phase** | **PHASE 2B BUILT, NOT PUSHED (2026-09-18) — official WhatsApp transport and human-approved outbound. Next: PHASE 2C — NOT STARTED.** Built on `feature/phase-2b-whatsapp-transport` from `feature/clinical-phase-1` at `1e582d43`. Local commits: the implementation `04a6b87c`, one focused pre-push review fix `25952e91` (report §17) and the ADR 0018 acceptance record; not pushed, so no CI run exists yet; `main` is unchanged, and there was no deploy. Read [docs/PHASE_2B_REPORT.md](docs/PHASE_2B_REPORT.md) and [ADR 0018](docs/adr/0018-whatsapp-transport-and-human-send.md) (amended by the review, **Accepted by the owner 2026-09-21** as written; the review's recommended addendum was not part of the acceptance). **What exists** (migrations `20260918150000_whatsapp_transport.sql` and `20260918170000_whatsapp_q8_gate_and_acknowledgement.sql`, forward only): the official Meta Cloud API, pinned to **Graph API v25.0** and verified against Meta's docs on 2026-09-18; `npm run whatsapp:gateway` (`node:http`, loopback by default), which checks `X-Hub-Signature-256` over the RAW bytes before parsing, then calls `ops.receive_whatsapp_message` / `ops.receive_whatsapp_status` as the **new role `ops_gateway`** (NOLOGIN; its login is `ops_gateway_login` via `npm run gateway:provision`; no table, exactly those two DEFINER functions, scrutinised by the static migration guard like `ops_worker`); `ops.communication_channels`: the owner maps one provider target (phone number id) to one tenant, company and agent, `mode` `test` or `production`. The target alone selects the tenant; a payload never does (SI-46); **the closed real-data gate**: `communication_channels_q8_real_data_gate` has no enabled value, so a production channel can exist only inactive (the configure service and CLI refuse first; dropping it is a static-guard finding), and `ENABLE ALWAYS` triggers refuse a transport admission or a send on any non-test channel, even to the owner (SI-47); **nothing is acknowledged in silence** (SI-53): a message is admitted, refused on the record as a content-free `communication.inbound_refused` fact, or UNROUTED (unknown, inactive or production target, or a paused unit) and answered 503 with nothing stored, so Meta keeps it; a test-channel message goes through Phase 2A's admission (now `ops.admit_inbound_core`, the same identity key) and becomes one task, one run and one review; `ops.conversations` groups by (tenant, channel, contact); every message stays its own task; `ops.crm_contact_by_phone` is the read-only CRM ContactPolicy: found, not_found, ambiguous or unavailable, exact on digits, no write (SI-48). **Outbound:** accepting a review still SENDS NOTHING. `npm run messaging -- send --review …` is a second, explicit operator act (owner credential + `WHATSAPP_ACCESS_TOKEN`). `ops.whatsapp_send_eligibility` runs at the request AND again immediately before the one call, under the kill-switch lock. It requires: the contact wrote within Meta's 24-hour window; exactly one CRM contact; an opt-out flag that is false (only the CRM's default: these are preconditions, not a lawful basis or consent); an active test channel; no global, tenant or company stop (SI-49). `ops.outbound_messages` is the durable state machine: `sending` commits before the call, the send is at most once, a 5xx, timeout, lost answer or Meta generic error code is `indeterminate`, and **nothing resends**; no retry command exists (SI-50). Status callbacks reconcile idempotently, inside the channel's tenant only (SI-51). No body, draft, number or secret reaches an event, an outbound row, a log or CLI output (SI-52). **No new dependency, no new job kind, no worker capability, no CRM write path.** **Local evidence after the review (not CI):** `test:db` 15/15 (new `whatsapp_transport.sql`, sections A–K); `test:db:engine` 240/240 in 32 files; `functions` 1638/1638; security invariants 60/60; migration guard 139/139; upgrade replay PASS; typecheck, lint, build, scan, production scope and signing key green; 18 deliberate guard breaks, each caught by name. **Not performed:** a live Meta call; there are no test credentials. The placement of `biz_opaque_callback_data` in a send is UNVERIFIED on Meta's pages, and a live test-number probe must settle it (report §13). **Owner decisions recorded, not made:** Q8; the LGPD basis for a service reply; retention and erasure of bodies (`ops.tasks.description`) and numbers (`contact_ref`); whether an unknown number may be answered; gateway hosting and TLS. **BASELINE Q8 is unchanged and still OPEN.** A test channel's message reaches the configured model provider exactly like a synthetic one does: connect only test numbers carrying test data. ~~Phase 2A current-phase text follows.~~ **PHASE 2A INTEGRATED (2026-09-18) — synthetic lead triage pilot. Next: PHASE 2B — NOT STARTED.** Phase 2A was built on `feature/phase-2a-synthetic-triage` (from `feature/clinical-phase-1` at `d2843913`) and merged into `feature/clinical-phase-1` by PR #4. The merge commit is `3a124a421ce8ab12a78c52b69cfaee41aa11f20a` and the final implementation head is `e5bef7530904ebac67785ccd03a70328a5ab340f`. The integrated commits are `35ea6c03`, `d944d860` and `e5bef753`. **Final reviewed CI, run 35377851431 on `e5bef753`:** `test:db` 14/14; `test:db:engine` 205/205 in 30 files (settlement regression 3/3, lead triage pilot 20/20); `functions` 1570/1570; security invariants 52/52; migration guard 131/131; upgrade replay PASS; typecheck, ESLint, build, secret scan, production scope and signing key green. Only the accepted baseline reds remain: e2e 9 failed / 1 skipped, and Prettier on `sampleCsv.test.ts` and `canAccess.test.ts`. There are no new regressions. `main` is unchanged, and there was no production deploy. **Owner decisions at merge (report §17):** the absence of a Codex review is accepted, and the legacy review trigger stays in place and INERT; do not delete it. The first end-to-end Company OS workflow, and deliberately the smallest one that proves the path. Read [docs/PHASE_2A_REPORT.md](docs/PHASE_2A_REPORT.md). **The flow:** a synthetic inbound message is admitted exactly once (`ops.admit_inbound_message`) → one `lead_triage` task carrying the message as its description → one explicitly requested agent run → the Phase 1D/1D.1 runtime executes it under the kill switch, the price, the spend limits and at-most-once external-call semantics → the validated advisory result opens a human review item, DERIVED from a run that SUCCEEDED, in a transaction of its own AFTER the settlement commits → a person decides through `npm run ops -- triage`, once, and the decision is final. **What was added** (migration `20260917190000_lead_triage_pilot.sql`, forward only): the capability `lead_triage` (standard route) and its output contract in `ops.agent_run_result_valid`; `ops.inbound_messages` (the admission ledger: identity + fingerprint, **no message body** — the body is the task's description, because that is what `ops.claim_agent_run` builds the prompt from); `ops.review_items`; `engine/communication/` (a `CommunicationPort` with **no send** and a synthetic adapter); `engine/models/leadTriage.ts` + `capabilityContracts.ts` (the handler now LOOKS UP a capability instead of naming one); `engine/domain/leadIntake.ts` and `reviewQueue.ts`; five `ops -- triage` subcommands; `npm run lead-triage:demo`. **No new job kind, no new worker capability, no new dependency.** **Boundaries, enforced rather than promised:** only `synthetic` messages are admitted and only when `COMPANY_OS_SYNTHETIC_INGRESS` is exactly `enabled`, and nothing served over HTTP reaches the ingress (SI-44); an inbound message becomes work at most once (SI-43); a model's answer never acts — accepting is refused for a do-not-contact lead, and accepting performs no action because **no outbound transport and no CRM write path exist** (SI-45). Phase 2A introduces no CRM mutation path. Existing worker capabilities may touch non-CRM public-schema infrastructure such as `public.inbound_emails`. **Focused pre-push review 2026-09-18 (report §15), one fix commit on top of the implementation commit, fix migration `20260918090000_lead_triage_review_recovery.sql`:** a review-queue failure no longer undoes a paid call's settlement (completed by the final review below; `npm run ops -- triage recover` opens a missing review from the stored result); consent comes ONLY from a trusted `ContactPolicy`, never the delivery (Phase 2B replaces the synthetic fixture with a read-only CRM lookup); consent is found by TASK, so a retry inherits it, and a task no admission created is do-not-contact; a redelivery is a pure lookup; the task title is a constant (no sender in the prompt); the body is traced and absent from every event, job payload, error and listing. Each behavioural fix was red on the unfixed schema and green after. **Final review 2026-09-18 (report §16), fix migration `20260918120000_lead_triage_review_after_settlement.sql`:** the §15 subtransaction did not isolate the settlement, because PL/pgSQL `when others` does not catch QUERY_CANCELED, so a statement timeout or a cancellation while the review opened rolled the paid settlement back (measured red twice, one of them a real lock wait from a second connection). The review is now opened AFTER TX2b commits, in a transaction of its own: `engine/worker/afterSettlement.ts` calls `ops.open_review_for_settled_job(worker, job)`, a job-bound SECURITY DEFINER for the worker that completed the job, pinned in `company_domain_core.sql` A4/A5. A failure is logged and `triage recover` opens the review; `CAPABILITY_NAMES` is unchanged. The old trigger is inert, not dropped: dropping an ops trigger needs an owner-approved exception to the static migration guard. **Evidence (local, after §16):** 14 SQL suites (G5 attempts every pilot object as each application role; three privilege mutants each caught by name; section I pins the post-settlement step), 205 driver-backed cases, 1570 `functions` unit tests, 52 security invariants, the upgrade replay, typecheck/lint/build/scan/production-scope green, and a reproducible demonstration. **`ops.tasks.description` holds the (synthetic) inbound body; real-message retention and minimisation must be revisited before Phase 2B and Q8.** **BASELINE Q8 is unchanged and still OPEN:** no real provider was called, the demo has no live mode, and every fixture is synthetic. Classification: PHASE 2A INTEGRATED — CLOSED (report §17). **Next: PHASE 2B — NOT STARTED.** The expected direction: the official WhatsApp Cloud API behind `CommunicationPort` (owner decision J); a read-only CRM `ContactPolicy`; an outbound action boundary that checks consent afresh at the moment it acts; and a decision on conversations and threading, where required. **Q8 must be resolved before real patient message content is sent to a real LLM.** ~~Phase 1D.2 current-phase text follows.~~ **Phase 1D.2 — pre-main safety closure: built 2026-09-17 on the branch `feature/pre-main-safety` (from `feature/clinical-phase-1` at `c0c07c37`), pushed by the owner. CI VERIFIED: run 35247254334 on `c00082fb`, where `test:db` passed 13/13 (before and after the clean reconstruction), `test:db:engine` 181/181, the upgrade replay passed, the migrations-only replay shipped no owner, the unit projects passed 2175 (`functions` 1421, security invariants 49/49), and typecheck, ESLint, the build and the secret scan are green; only the pre-existing `e2e-test` and Prettier checks are red, identical to the baseline run 35220094426. Not merged.** A debt-closure phase only: it settles four Codex findings on PR #1 before `main`, adds no product functionality and does not start Phase 2A. Read [docs/PHASE_1D2_REPORT.md](docs/PHASE_1D2_REPORT.md). **Findings 1-3 (in `20260911232039_pending_delta.sql`, sealed) were reproduced on legacy data and fixed by forward migrations `20260917180000`-`180300`:** legacy deals keep their stage (identity backfill, `updated_at` preserved); every legacy contact gets exactly one lead profile, so an opt-out can be recorded (SI-42); legacy administrators are neither silently dropped nor promoted on trust — the upgrade HALTS until a person runs `public.bootstrap_owner` (a recorded act with the database credential, also the fresh-deployment owner path; runbook PERMISSIONS.md §3; SI-41). **Finding 4 was confirmed from source and fixed:** the live-database gate is one reusable workflow, `.github/workflows/database.yml`, called by `check.yml` and by `deploy.yml`, where `deploy-supabase` now `needs: [gate, database]` (SI-40). **New:** `npm run test:db:upgrade`, an upgrade replay over legacy data that runs in that gate and resets its stack; `supabase/tests/owner_provisioning.sql` and `crm_data_invariants.sql` (13 `test:db` suites). **Owner review 2026-09-17: decisions F-H ACCEPTED** (report §16; DECISIONS.md): **F** legacy `administrator = true` rows are NEVER promoted automatically — a production upgrade can intentionally STOP at `20260917180300` and is resumed only after a person runs `public.bootstrap_owner` (a two-step operational upgrade; do not weaken it); **G** the deal-stage repair keeps the business `updated_at`; **H** a backfilled lead profile's `acquired_at` is derived from the contact's earliest evidence and documented as derived. **Production-readiness gate (not this phase, does not block its CI, does block a real deployment where it applies):** `users`/`patchUser` and its half-state administrator paths, `delete_note_attachments`, committed development-secret debt, the makefile deploy path without the database gate; BASELINE Q8 is separate and still blocks real patient data reaching an LLM. Classification: CI VERIFIED (report §17); Phase 2A NOT started, it waits only for its own brief. ~~Phase 1D.1 current-phase text follows.~~ **Phase 1D.1 — runtime governance closure: built 2026-09-16/17 on the branch `feature/runtime-governance` (from `a2d0c37c`), committed and pushed 2026-09-17 (four phase commits and the owner decision E commit `b4dee040`). CI VERIFIED: run 35220094426 on `b4dee040`, where `test:db` passed 11/11 (before and after the clean reconstruction), `test:db:engine` 181/181, the `functions` project 1414 (security invariants 46/46), the harness guards, typecheck, ESLint, the build and the secret scan are green; only the pre-existing `e2e-test` and Prettier checks are red, identical to the baseline run 35129049291.** Read [docs/PHASE_1D1_REPORT.md](docs/PHASE_1D1_REPORT.md) and [ADR 0017](docs/adr/0017-runtime-governance.md) (**Accepted by the owner 2026-09-17** with an owner-review addendum: A fail closed without price, ceiling or budget; B a stopped job is held or deferred, never cancelled, and runs only after an explicit clear; C a request may wait, bounded, for a prepare in progress; D the spend-ceiling stop is cleared only by a person; E a new request under an active stop is refused and recorded, with no job, no provider call and nothing deferred). **What exists:** versioned owner-recorded model prices (`ops.model_prices`: never in a migration, never used past `expires_at`, no fallback to an older version); cost derived by the database (`price_id`, `reserved_cost_micros`, `estimated_cost_micros`, `charged_cost_micros` on `ops.agent_runs`; an unknown outcome stays charged at its reservation); versioned daily spend limits (`ops.spend_limits`: global ceiling + tenant budget REQUIRED, company optional) checked in `ops.start_agent_run` under per-scope advisory locks after the kill-switch lock (settled exhaustion is recorded `budget_exhausted`; contention with calls in flight raises `OS429` and retries); a global ceiling exhausted by settled spend trips a `system:spend_ceiling` global stop (stops now carry `origin`); the ONE kill switch now holds every non-internal job at the lease without consuming an attempt, has a `job_kind` scope, and the `external_call` runtime (`engine/worker/externalCall.ts`) re-checks it before any call and defers the job; a stop that `ops.start_agent_run` finds holds the run (it answers `stopped`, the run stays `pending` with nothing recorded) and the runtime defers its job with its attempt given back, so the same run runs after an explicit clear (owner decision B; a request made under a stop is still refused before any job exists, owner decision E: a stop before admission refuses new work, a stop after admission holds it); `ops.create_task`/`ops.record_event` take an optional idempotency key; `npm run ops` (operator CLI, read-only by default, with three narrow acts: record a price, set or retire a limit; `ops.spend_status()` reports `settled_exhausted` and `new_run_admission` (`blocked` or `conditional`), and `status` reports `settledExhausted`, `newRunAdmission` and `globalCeilingConfigured`). **Evidence (local):** 11 SQL suites (new `runtime_governance.sql`), 181 driver-backed cases in 28 files, 1414 `functions` unit tests, typecheck/lint/build/scan/production-scope green; adversarial design review + per-stream reviews + mutation testing. **BLOCKER: BASELINE Q8 — no real patient message body, clinical text, psychotherapy information or health data may reach a real LLM provider until the owner decides Q8; synthetic data is allowed, and Phase 2A is synthetic-data only until then.** A deployment refuses every agent run until a price, a global ceiling and the tenant budget are configured. Classification: READY FOR PHASE 2A (synthetic data only) — CI VERIFIED. **Next: PHASE 2A — SYNTHETIC LEAD TRIAGE PILOT, direction accepted by the owner 2026-09-17, NOT started** (see ROADMAP); it waits only for its own brief. ~~Phase 1D current-phase text follows.~~ **Phase 1D — agent runtime, model router and the first model call: built 2026-09-14, committed and pushed 2026-09-16 (four Phase 1D commits, the ADR 0016 owner-review amendment `0a4bcfec` and the CI fix `c4382b9b`, on top of `60cbc2b5`). CI VERIFIED: run 35129049291 on `c4382b9b`, where `test:db` passed 10/10 (before and after the clean reconstruction), `test:db:engine` 85/85, the unit projects, typecheck, ESLint, the build and the secret scan are green. Only the pre-existing `e2e-test` and Prettier checks are red, identical to the baseline run 34836911824. The first run, 35125528392 on `0a4bcfec`, failed two Phase 1D test-surface checks: a seed-file mention in `engine/cli/agentRunSmoke.ts`, and a driver test that hard-coded CI's own database as "another" one. `c4382b9b` fixed both.** Read [docs/PHASE_1D_REPORT.md](docs/PHASE_1D_REPORT.md) and [ADR 0016](docs/adr/0016-agent-runs-and-model-providers.md), **Accepted by the owner 2026-09-16** with an amendment: an outcome is `failed` only when the provider was never called or its answer settles it, and every ambiguous outcome, a 5xx included, is `indeterminate`. **What exists:** `ops.agent_runs` — one bounded model invocation, for one agent, about one task it is assigned, requested only through `ops.request_agent_run`, at most once, and inert. The pieces: a six-edge state machine with `indeterminate`; database-owned correlation and causation; tenant-scoped idempotency; the `external_call` worker handler shape; six lease-bound DEFINER capabilities; a provider-neutral `ModelProvider`, an OpenAI Responses adapter over `fetch` and a fake for CI; deterministic tier routing; strict structured output validated twice; usage recorded and no cost; and the minimal kill switch (`ops.execution_stops`, `npm run execution-stop`). **Evidence:** 10 SQL suites and 85 driver-backed cases, including crashes through killed worker processes, green; every load-bearing guard mutation-tested; SI-28 to SI-34 added, SI-21 and SI-24 restated. **No live provider call has been made, no tools, no memory, no UI, no CRM access.** Classification: READY FOR NEXT PHASE — CI VERIFIED. The report's proposed Phase 2A scope is NOT accepted: the next milestone is decided in a separate roadmap review. *(2026-09-17: the roadmap review chose Phase 2A — synthetic lead triage pilot; see ROADMAP.)* ~~Phase 1C current-phase text follows.~~ **Phase 1C, the Company OS domain core: built 2026-09-12, signed off 2026-09-13, CI VERIFIED (run 34770182266 on `e160debb`; only the pre-existing `e2e-test` and Prettier checks are red, identical to the baseline). Phase 1C and ADR 0015 ACCEPTED by the owner 2026-09-13, with an addendum: tenant is the only isolation boundary, businesses that need independent data isolation are separate tenants (a tenant may still hold several companies, as organisation only), `events.seq` internal only, minimised event payloads, and neither `postgres` nor `service_role` is ever an ordinary agent or worker identity. ~~Phase 1D waits on the owner's review of ADRs 0002 and 0003; ADR 0002 is blocked on ADR 0011 item 4 (the MCP function still pools as `postgres` while `ops` exists).~~ Pre-1D closure 2026-09-13: ADR 0003 ACCEPTED as reconciled; the generic MCP SQL function REMOVED and kept out of the committed deploy paths (SI-03); the development seed kept off hosted projects (SI-25); global monotonic identifiers internal only (SI-26). ~~ADR 0002 stays Proposed: the `merge_contacts` owner-session Postgres pool has no test against `ops`, and the closure commits await CI.~~ Final pre-1D closure 2026-09-13: ADR 0002 ACCEPTED under the owner's acceptance bar. The `merge_contacts` owner-session pool is measured, through the real deno-postgres driver, unable to reach `ops` or carry state between requests (SI-27). Every deploy first checks which functions the hosted project serves (SI-03). Global reference data ships in migrations, while tenant vocabulary waits for onboarding (SI-25). ~~CI has not yet run on the closure commits.~~ CI VERIFIED 2026-09-14: run 34836911824 on `a1f9bbca`; only the pre-existing `e2e-test` and Prettier checks are red, identical to the baseline run 34779599564 on `b155b177`.** Read [docs/PHASE_1C_REPORT.md](docs/PHASE_1C_REPORT.md), then [docs/SECURITY_INVARIANTS.md](docs/SECURITY_INVARIANTS.md) and [ADR 0015](docs/adr/0015-company-os-domain-core.md), which is Accepted with the owner addendum. **What exists:** tenant → company → department → agent → task → event, in `ops`. **Integrity:** composite keys make cross-tenant and cross-company rows unstorable. **Tasks:** an 11-edge state machine lives in an `ENABLE ALWAYS` trigger. **Events:** every lifecycle change writes exactly one, in the same statement. **Execution:** a task → job bridge exists with an **empty** allowlist, so no task can cause execution. **Access:** backend-only; no application role holds any privilege on it. **Evidence:** 41/41 database mutations caught by their named assertion; the Data API probe (REST and GraphQL, 5 credentials) reaches nothing in `ops`; 1129 unit tests, 6 database suites and 42 driver-backed cases. **Local stacks:** they stay on loopback only because Docker Desktop's Port binding behavior is "Localhost only" (see Known issues). Earlier phases: [docs/PHASE_1B_REPORT.md](docs/PHASE_1B_REPORT.md), [docs/SECURITY_AUDIT_1BS_REPORT.md](docs/SECURITY_AUDIT_1BS_REPORT.md). ~~**No agents, no LLM, no UI. Phase 1D has not started.**~~ *(2026-09-14: Phase 1D is built; see the start of this cell.)* |
| **Start here** | **[docs/SECURITY_INVARIANTS.md](docs/SECURITY_INVARIANTS.md)** (the properties that must hold, each with the guard that proves it) → [docs/PHASE_0_5_REPORT.md](docs/PHASE_0_5_REPORT.md) → [docs/BASELINE_REPORT.md](docs/BASELINE_REPORT.md) → [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) → [docs/SECURITY.md](docs/SECURITY.md) → [docs/PERMISSIONS.md](docs/PERMISSIONS.md) → [docs/ROADMAP.md](docs/ROADMAP.md) → [docs/DECISIONS.md](docs/DECISIONS.md) |

**Open-source-first, adapter-first** *(owner strategy, 2026-09-17)*. A mature capability that exists as genuinely free, self-hostable open source is integrated **behind one of our adapters**; anything else is built with Claude Code. Source-available, fair-code and Enterprise-gated software does not qualify, and AGPL/GPL is flagged rather than adopted silently. **Once a component is APPROVED for implementation, cloning, downloading, running, modifying and forking its upstream repository is allowed and encouraged where it materially reduces engineering work** (owner decision 2026-09-17, review §15, approved as written): check the license of the exact files reused, never import from an Enterprise directory, keep the notices, pin a tag and commit, and record provenance. **Fork is not the default** — the mode follows the shape: library → package, large standalone application → external service behind an adapter, substantial persistent source modifications → fork, small clearly permissive module → source extraction, adoption dearer than building → reference only and build it ourselves. The objective is not avoiding forks; it is the lowest total implementation + maintenance + upgrade + exit cost. The first real adoption creates `docs/OSS_PROVENANCE.md` with the twelve fields in review §15.3 — no placeholder exists before then. Cloning candidates merely to evaluate them during a research review is not allowed. Adding an npm dependency stays gated by `.claude/rules/dependency-safety.md`. The engine, tenancy, domain, governance and spend accounting are never replaced. See [docs/ARCHITECTURE_ACCELERATION_REVIEW.md](docs/ARCHITECTURE_ACCELERATION_REVIEW.md) — **owner decisions I–O ACCEPTED 2026-09-17** ([docs/DECISIONS.md](docs/DECISIONS.md)): integrate soon — pgvector, OpenTelemetry, Prometheus, Umami, Playwright, with observability in Phase 2D; rejected — n8n, Evolution API, WAHA Plus, Arize Phoenix and every unofficial WhatsApp gateway, so the patient channel is the **official Meta Cloud API behind our own `CommunicationPort`**; deferred — an omnichannel inbox is not a Phase 2 goal and Grafana would be an internal unmodified viewer only; **Phase 2A adds no dependency**, and Q8 stays open.

## The five rules that override convenience

1. **Atomic CRM is a replaceable adapter, not the foundation.** Business logic must never depend on Atomic CRM internals — no `ra-core` types, no PostgREST filter syntax (`field@ilike`, `tags@cs`), no view names (`contacts_summary`), no JSONB email/phone shapes, no bigint FKs into `public.*` from engine code.
2. **Nothing in the engine may be psychology-specific.** Tenant vocabulary — pipeline stages, department names, task types, loss reasons — is **data**, never DDL and never a prompt constant. ✅ The two CHECK constraints that violated this (`deals.pipeline_stage`, `lead_profiles.operational_status`) were **removed 2026-09-11** before any migration froze them — see [ADR 0013](docs/adr/0013-pipeline-stages-are-configuration.md). The test for a new CHECK: *would tenant two need a different set?*
3. **Deterministic code owns anything code can do reliably.** Arithmetic, permission checks, risk evaluation, budget checks, CRUD, state transitions, threshold detection. LLMs own language, interpretation, classification where rules are insufficient, analysis and recommendation.
4. **Fail closed.** Every gate denies by default. ✅ `canAccess` was inverted to an explicit allow-list on 2026-09-11 (unknown resource → denied, 24 tests). The harness approval hook still exits 0 when it cannot identify a ticket. Port those shapes, invert the defaults.
5. **Functionality first.** Phase 1 UI is deliberately ugly: tables and cards. No animations, avatars, isometric environments or cosmetic refactors. Architecture, reliability, data integrity, security, observability, cost efficiency.

## Known issues

Verified 2026-09-10, **re-verified 2026-09-11 against `729f5966`**. Do not mistake any of these for something you introduced. The working tree is **clean** — an earlier version of this section said it was broken and blamed uncommitted work; that is no longer true.

**Environment — check these two before starting anything:**
- ⚠️ **Docker works, but broke once in a way worth knowing.** Docker Desktop failed to start with `rename ...sock -> ...sock.stale: The file cannot be accessed by the system` across two independent subsystems. Probable cause: **three stacked antivirus products** (Norton 360 running, Kaspersky, Defender) and their filesystem minifilters. If it recurs, add AV exclusions for `%LOCALAPPDATA%\Docker`, `%LOCALAPPDATA%\docker-secrets-engine` and `%ProgramData%\Docker` before anything else. Always run `docker info` first.
- ✅ **`.husky/pre-commit` no longer blocks commits** (fixed 2026-09-11). It called `make registry-gen`, `make` is absent, and `.husky/_/h:17` runs the hook under `sh -e` — so it aborted at line 1 with code 127 and `npx lint-staged` never ran. The hook now calls `npm run registry:gen` and prettier directly. **`make` is still not installed: use `npm run` / `npx` directly, never `make`.**
- ⚠️ **`registry.json` is deliberately left stale.** `npm run registry:gen` was silently destroying it on Windows (223 files → 1: `path.join` emits backslashes and `glob` treats `\` as an escape). That is fixed in `scripts/generate-registry.mjs`, and so is a **second Windows defect found 2026-09-11**: `globSync` RETURNS platform separators, so every emitted `path` read `src\components\atomic-crm\types.ts` — published content that `shadcn add` cannot resolve. The first fix corrected the glob *patterns*; this one corrects the *output* (`toPosixPath`). The generator is now correct — but running it adds `LeadCommercialPanel.tsx` to the **published** registry, which is the exposure [ADR 0008](docs/adr/0008-fork-posture.md) exists to decide. Your next commit will regenerate it via the hook. Settle ADR 0008 first, or drop the `registry:gen` line from the hook.
- ✅ **Local Supabase must stay on loopback — mitigated and verified 2026-09-12.** The Supabase CLI publishes every port with no host address, and Docker Desktop's default ("Open") then listens on every interface, IPv4 and IPv6. Before the fix, both local stacks answered on the LAN address, the WSL adapter and this machine's global IPv6 addresses, with three unauthenticated ways to run SQL as `postgres`:
  - Kong `POST /pg/query` (54341);
  - Studio `POST /api/platform/pg-meta/default/query` (54343);
  - Postgres itself (54342, default password).

  **The fix, applied on this machine:** Docker Desktop → Settings → Resources → Network → **Port binding behavior → "Localhost only"**, then recreate each stack: `npx supabase stop --workdir .supabase-e2e`, then `start`.
  - **Effect on existing containers:** on Docker Desktop 4.89 the running containers were already rebound to `127.0.0.1`/`[::1]` as soon as the setting was applied. Recreate them anyway, then run the check.
  - **Verified after the change:**
    - every published port listens only on `127.0.0.1` and `[::1]`;
    - the SQL paths still answer locally;
    - every LAN, WSL, link-local and global IPv6 address refuses;
    - the WSL VM cannot connect;
    - containers still reach the host through `host.docker.internal`;
    - `db reset`, `test:db` (6/6) and `test:db:engine` (42/42) pass.
  - **What it costs:** Docker now refuses an explicit `-p 0.0.0.0:…` publish for every project on this machine ("for local-only port binding, the host IP must be a loopback address").

  **What does not work, measured:**
  - A network created with `com.docker.network.bridge.host_binding_ipv4=127.0.0.1` and passed via `--network-id` (Supabase's own documented recipe): Docker Desktop still published on `0.0.0.0` and `[::]`.
  - daemon.json `"ip"`: it applies only to Docker's default `bridge` network.
  - A Windows Defender Firewall rule is not a dependable substitute here. Norton 360 and Kaspersky are registered firewalls, and traffic from the WSL VM reached every port despite Defender's "block inbound" default.

  **Detect a regression:** run `npm run check:local-exposure` (exit 0 loopback-only, 1 exposed, 2 unverified). `npm run test:db` prints a warning before its suites and again after its summary whenever its stack is exposed; a CI runner (`CI=true`) skips it.
- ⚠️ **Git Bash rewrites container paths.** `docker exec … psql -f /tmp/x.sql` has `/tmp/…` converted to a Windows path and fails. Pipe SQL on stdin, or prefix the command with `MSYS_NO_PATHCONV=1`.
- ⚠️ **`.supabase-e2e/supabase/migrations/` and `.supabase-e2e/supabase/seed.sql` are COPIES.** Editing a migration or the seed under `supabase/` changes nothing in the e2e stack until you copy it across. A reset without re-syncing silently measures the old schema.
- ⚠️ **`npm run test:db:engine` needs the e2e stack named locally:** `SUPABASE_DB_CONTAINER=supabase_db_atomic-crm-e2e SUPABASE_DB_PORT=54342`. Without them it defaults to port 54322 — the other working copy's database — and on this machine fails at provisioning (`spawnSync psql ENOENT`) before any test runs. CI needs neither: it has one stack.

**Custody and CI:**
- **The branch is pushed, and CI runs on it.** `check.yml` triggers on `feature/**`, and the Phase 1B and 1B-S reports cite their runs. Agents still never push (`.claude/rules/git-policy.md`), so a commit an agent makes has no CI result until the owner pushes it.
- ✅ **`deploy.yml` is gated on a passing build** (2026-09-11). A `gate` job runs typecheck, lint and all three unit projects; every deploy job carries `needs: gate`. The `claude` project also runs in CI now — it never did, which is how five silent hook defects shipped. *(2026-09-17, Phase 1D.2: that gate ran no database suite, and `needs:` cannot reach `check.yml`, which is another workflow run. `deploy-supabase` now also needs a `database` job calling `.github/workflows/database.yml` — the one definition of the live-database gate, which `check.yml` calls too. `scripts/production-scope.mjs` refuses a hosted push that does not directly need it, SI-40.)*
- **`npm run build` does not typecheck.** `tsconfig.json` is solution-style (`"files": []`), so the bare `tsc` compiles nothing (verified: `tsc --noEmit --listFiles` emits 0 lines). **`npm run typecheck` is the only real gate** — and it covers only `src`, `demo`, `vitest-browser.d.ts`; `e2e/`, `supabase/functions/`, `.claude/` and `scripts/` are typechecked by nothing. *(2026-09-14: `tsconfig.app.json` also includes `engine/`, so the worker, models, handlers, domain and their driver-backed tests are typechecked.)*

**Database:**
- ✅ **The schema rewrite reaches the database** (2026-09-11). `[db.migrations] schema_paths` is wired and confirmed working; the delta was generated, reviewed statement by statement, and applied. A clean `supabase db reset` rebuilds everything in ~35s, run twice consecutively.
- 🔴 **Never apply `supabase db diff` output unreviewed.** It is not empty and cannot be made empty. It emits `create extension pg_net` (re-opening an exfiltration channel that `anon` can reach and that no privilege change of ours can close) and recreates three views **without `security_invoker`** — `migra` never emits view reloptions. That second behaviour already shipped an unauthenticated read of every contact record once; see PHASE_0_5_REPORT §16.1. Both regressions are caught by `npm run test:db`.
- ⚠️ **Anything `db diff` cannot express must be a hand-written migration, and this has bitten the project three times.** `db diff` emits DDL only, so `update storage.buckets` (`07_storage.sql`), `ALTER DEFAULT PRIVILEGES` (`06_grants.sql:62-67`) and a view's `security_invoker` reloption (`03_views.sql`) are all declared in the repository and present in **no** generated migration. Each is now carried by a hand-written migration with a `raise exception` assertion. Before adding anything to `supabase/schemas/`, ask whether `db diff` can actually emit it.
- ✅ **`supabase db reset` works** (verified 2026-09-11, two clean runs, 36s/34s). The `loss_reasons` blocker was the missing delta migration, now generated as `20260911232039_pending_delta.sql` — 1114 lines, **reviewed statement by statement in Phase 0.5C** (269 statements; the review found the unauthenticated contact read described above). ⚠️ **A second working copy at `D:\download\CRM` shares this repo's `project_id`, so its containers AND VOLUMES collide. Always verify with `--workdir .supabase-e2e` (project `atomic-crm-e2e`, ports 5434x) or a `db reset` here destroys that project's database.**
- **A deployed instance is locked out.** `authProvider.ts:61` selects `sales.role`, a column in no migration → PostgREST 42703 on every login. **Latent, not live** — the owner confirmed no hosted Supabase project exists.
- ~~**Two different broken auth models, in opposite directions.**~~ *(Superseded 2026-09-17, Phase 1D.2: since `20260911232039` the migrations carry the owner model too, and the first owner comes from `public.bootstrap_owner`, a person's act; an upgraded database with legacy administrators halts until it runs. See PERMISSIONS.md §3. The text below describes the state before that.)* In `supabase/schemas/` no code path can create the first owner (`is_admin()` requires `role='owner'`; the trigger hardcodes `operator`; signup is off; `authenticated` has no INSERT on `sales`; the bootstrap UI was deleted). But the only `is_admin()` in `supabase/migrations/` tests `administrator = true`, and the deployed trigger makes the first signup an administrator — so a *migrated* database has a working admin and no deadlock. Always say which of the two you are fixing.

**Security — verified 2026-09-11, not previously recorded.** The canonical list is **[docs/SECURITY_INVARIANTS.md](docs/SECURITY_INVARIANTS.md)**, whose executable form is `supabase/tests/securityInvariants.test.ts`: 52 invariants (SI-01 to SI-53; SI-07 retired 2026-09-13; SI-27 added by the pre-1D closure, SI-28 to SI-34 by Phase 1D, SI-35 to SI-39 by Phase 1D.1, SI-40 to SI-42 by Phase 1D.2, SI-43 to SI-45 by Phase 2A, SI-46 to SI-53 by Phase 2B), each tied to a guard that actually runs, and a test that goes red by name if a guard is deleted, renamed or softened. The notes below are the *measurements* behind them — read the invariants first, these for the why.

- ✅ **`validateSql` DML bypass closed, BOTH layers verified** (2026-09-11). The classifier recurses into both halves of a `WITH` (7 adversarial tests, mutation-verified), and `SET TRANSACTION READ ONLY` was exercised against real Postgres: `with x as (select 1) delete from contacts` is refused with `cannot execute DELETE in a read-only transaction` **even with the validator removed**. *(2026-09-13: moot — the MCP function and `validateSql` are removed; SI-03.)*
- ✅ **`extensions.*` egress CLOSED and verified** (2026-09-11). Note what did NOT work: `REVOKE EXECUTE` is **silently ignored** — those functions are owned by `supabase_admin` and migrations run as `postgres`, which cannot revoke another role's grant. The effective fix is `REVOKE USAGE ON SCHEMA extensions` (postgres owns the schema). **And the documented fear that this breaks `citext` filtering is FALSE — measured: equality, ILIKE and INSERT on citext columns all still work.**
- ~~🔴 **`net.http_get(...)` STILL executes as `authenticated`** — verified. Schema `net` is owned by `supabase_admin`, so **no migration can revoke it**. Outbound HTTP from inside the database remains possible. The mitigation is ADR 0011 (stop exposing arbitrary SQL), not a grant.~~ ✅ *(Corrected 2026-09-13: `pg_net` was dropped on 2026-09-11 by `20260911235500_drop_pg_net.sql`, which takes `net.http_get` with it and is asserted by SI-02; the MCP SQL function was removed on 2026-09-13, SI-03.)*
- ✅ **`merge_contacts` data loss fixed AND verified against a real database** (2026-09-11). The bug was reproduced (loser's profile + attributions cascaded to 0, `do_not_contact` lost) and the fix proven (attributions preserved, consent `true`). Consent is `OR`, never winner-wins, and symmetric.
- ✅ **The Postmark webhook no longer returns 200 on every ingestion failure** (2026-09-11). `addNoteToContact`'s error Responses are now classified (403 → permanent, everything else → transient), recorded in `public.inbound_emails` with the raw payload, and folded across recipients into the delivery's status. 49 tests, green, and the two guards added after adversarial review are mutation-verified. The ledger migration **has been applied and verified** on a local Supabase (RLS on; `anon` holds nothing on the table or its sequence; `authenticated` SELECT only, gated on `is_admin()`; unique index present).
- **Tag creation and settings writes are dead** on any deployment of this schema: every policy gated on `is_admin()` is unreachable, while the grants hand those verbs to `authenticated`.
- ✅ **Postmark ingestion is durable** (2026-09-11), on the second attempt — the first was rejected for shipping incomplete files and its own fail-open (PHASE_0_5_REPORT §2.4). Ingested/duplicate → 200, permanently invalid → 200 **plus** a ledger row holding the raw payload, transient → 500. Idempotency is claim-before-work against a unique index on `(message_id, recipient_email)` — **not** `recipient_contact_id` as the design said: the contact does not exist at claim time, and a NULLable column in a unique index deduplicates nothing ([ADR 0014](docs/adr/0014-inbound-email-ledger-keys-on-recipient-email.md)). The recipients loop is now a callback over `collectRecipientResults`, so the rejected attempt's early-return bug is unrepresentable. ⚠️ **`public.inbound_emails` has never been created in any database** — the migration is unapplied (Docker).

**Tests:**
- ✅ **The `app` project is green** — 189 passed, 1 skipped, 0 failed (fixed 2026-09-11; it had 13 failures, all in files `2b5f20bb` touched). Two were real product bugs, not stale tests: the deal importer grouped by `pipeline_stage` while the FakeRest generator only set `stage`, and the demo provider had no `lead_profiles` collection at all. Both are fixed in `providers/fakerest/dataGenerator/`.
- **`vi.mock` does not mock modules under the browser-mode runner.** `vi.mocked(x).mockResolvedValue` throws `is not a function`, and any un-mocked call then hits the real network. Stub the global `fetch` instead (`vi.stubGlobal`) — `misc/fetchWithTimeout.ts` wraps it, so one seam covers both paths. `providers/commons/getContactAvatar.test.ts` is the worked example.
- ✅ **The `claude` project went from 47 failed / 178 passed / 28 skipped to 253 passed / 0 failed** (2026-09-11). One file, `.claude/scripts/test/pending-deploys.test.mjs`, still fails to **collect** (see below) — its tests do not run, which is why the file count reads 32/33.
- **What that fixed was five real hook defects on Windows, not just red tests.** Every one of them failed silently in production:
  - `cleanup-worktree.mjs` — **deleted live worktrees, including uncommitted developer work.** `git worktree list --porcelain` prints POSIX separators on every platform; the hook compared them against `join()`-built paths, so `registered.includes(dir)` was never true and the `rmSync(dir, {recursive:true, force:true})` guard never fired. `dir.endsWith("/_session")` failed the same way, so the merger's `_session` worktree was swept too. Paths are now absolutized **and** separator-folded (`resolve()` is required: `TMP_ROOT` defaults to the literal `"/tmp"`, which Windows turns into a drive-relative path while git reports an absolute one).
  - `restrict-documentator-write.mjs` — the allowlist degenerated to "deny everything", so the documentator could not write `MEMORY.md`, its ledger, or anything under `CONFIG_DIR/local/`. The separator fold is gated on `process.platform === "win32"` **deliberately**: `\` is a legal filename character on Linux, and an ungated fold would let `/…/local\evil/x.md` satisfy the allowlist — a privilege escalation on the harness's real deployment target.
  - `link-session-workspace.mjs` — `symlinkSync` needs Administrator or Developer Mode on Windows; the failure was swallowed as "skipped". Now creates a junction, which needs no privilege.
  - `check-config-sync.mjs` and `pending-deploys.mjs` — their `import.meta.url === \`file://${process.argv[1]}\`` main-guards are never true on Windows, so both scripts exited 0 without doing anything. Every caller read that as success, so the config-sync gate and the orchestrator's migration gate were **silently skipped**. Both now use `pathToFileURL`, guarded on `process.argv[1]` being present (it throws on `undefined`, and both modules have public exports).
- ⚠️ **The same root cause is still live in three places, deliberately left unfixed** (fixing them is not a Windows-compat bugfix of a failing test, so it needs its own decision): `.claude/hooks/lib/validation.mjs:16` — `getActiveWorktrees()` returns `[]` on Windows, so **`validate-on-stop` silently validates nothing**; `.claude/hooks/cleanup-session.mjs:69,75` — session teardown removes no worktrees; and `cleanup-worktree.mjs`'s `removeWorktreeFolders(...)` call, which compares git-form paths against the `join()` form `setup-worktree.mjs:252` registered.
- ⚠️ **`.claude/scripts/test/pending-deploys.test.mjs` does not collect.** `pending-deploys.mjs` is a `755` executable whose first line is a `#!` hashbang; Vite 7 preserves it and Vitest wraps the module in a function body, so `#!` is a `SyntaxError`. The fix needs a `server.deps.external` entry in `vitest.config.ts`, which is outside `.claude/` and was deliberately not taken. Note `check.yml` runs only `test:unit:app` and `test:unit:functions`, so the `claude` project is **never run in CI** — which is why none of this was caught.
- ⚠️ **The `app` project is load-sensitive.** It runs in a real Chromium. A full-suite run started immediately after `supabase db reset` produced 7 spurious failures across 7 files; it did not reproduce (810 passed next run, and 3 consecutive isolated `app` runs at 219 each). Do not run the browser suite concurrently with a Supabase start/reset, and do not read that shape as a regression. CI is unaffected — `test-app` and `database` are separate jobs.
- **Run the app suite as `npx vitest run --config vitest.config.ts --project app --reporter=dot`.** It finishes in ~77s. Piping the default reporter into `tail`/`head` makes it appear to hang for over an hour — that is a TTY artifact, not a slow suite.
- **The fork's 65-file change set added zero tests**, and that debt is now largely paid: `canAccess.ts` has 24, and the 43-policy rewrite is covered by `supabase/tests/rls_tenant_isolation.sql` (`npm run test:db`), which is mutation-verified against 14 deliberate policy breaks.

- **Never grant the worker a write verb, and never put `ops_worker` in `BYPASS_ROLES`.** The whole isolation rests on it: every job transition goes through a `SECURITY DEFINER` function that checks the lease first, so a worker that could write `ops.jobs` could extend its own lease or settle another tenant's job. The static migration guard rejects such a grant before it applies.
- **Tenant context is NOT a GUC you set.** `ops.current_tenant_id()` resolves it from a live lease (`app.worker_id` + `app.job_id` must name a leased, unexpired row owned by that worker). Measured: a worker CAN write any GUC, so a bare `app.tenant_id` would make tenancy an assertion by the worker. Call `ops.lease_job()` or `ops.resume_lease()`; never set the context by hand.
- **The lease commits in its OWN transaction, and that is load-bearing.** Phase 1A leased and executed in one transaction; measured, a worker that dies mid-job then rolls the lease back too, `attempts` returns to 0, and a job that crashes the worker is re-leased forever. Do not "simplify" the three-transaction shape in `runOneJob.ts` back into one.
- **The worker never gets a database client.** A handler receives a frozen capability object holding exactly what its registry entry declared. Adding a capability is a review event (`CAPABILITY_NAMES` is pinned by a test). Never pass `tx` to a handler, and never give a capability a tenant argument.
- **`ops_worker` is NOLOGIN on purpose.** Deployments run `npm run worker:provision` to create `ops_worker_login` (NOINHERIT, no BYPASSRLS). NOINHERIT is what makes `set local role ops_worker` load-bearing. Note `alter role ... nosuperuser|nobypassrls` needs a real superuser and Supabase's `postgres` is not one - the script asserts instead of stripping.
- 🔴 **Never use `supabase/signing_keys.json` for a hosted Supabase project.** It is upstream's committed DEVELOPMENT key and it contains the PRIVATE component. Measured in the 1B-S audit: a JWT signed with it and claiming `role: service_role` is accepted by PostgREST and bypasses RLS entirely. Harmless locally (hosted projects generate their own keys); catastrophic if reused. The security gate's PASS classification is void if this is ever done. **Enforced since the 1B-S closure (SI-20):** `node scripts/dev-signing-key.mjs` refuses any copy of the key outside its file (verbatim, base64, hex, PEM, or its public half) and any mention outside four local-tooling lines. Every GitHub Pages publish must go through `scripts/publish-pages.mjs`, which scans first. Every `supabase db push` / `functions deploy` / `secrets set` in `deploy.yml` or the makefile must follow a blocking `--project-ref` / `--linked` check, which asks the project's own JWKS whether it trusts the key.
- **Secrets must never reach the frontend bundle.** `npm run scan:build` (a required CI step) refuses a build containing a service_role JWT, `sb_secret_*`, a PG connection string, a private key (PEM or JWK), the development signing key, or a GitHub token. The publishable/anon key is expected and is NOT flagged. Only `VITE_`-prefixed variables reach the bundle - proven by building with a service_role key in `.env.e2e` and finding it in no artifact.
- **CRM record data must never be persisted in the browser (SI-19).** The React Query cache is memory-only. Upstream's mobile "offline mode" persister wrote every viewed contact, note, email address and consent flag to `localStorage` for 24h, and no product requirement needs offline access, so it was removed. `@tanstack/*persist*` imports are lint errors, and `root/CRM.security.test.tsx` measures storage on both admin trees. `providers/queryCacheKey.ts` now exists only to purge a cache an earlier build left behind. Anything else cached in `localStorage` must still be cleared on logout, and typed list filters already persist until then (SEC-1BS-13).
- **Anything hashed or compared byte-for-byte must normalise line endings.** git checks this repo out CRLF on Windows and LF on Linux. The migration seal was computed on Windows and failed every sealed file on the first CI run. `loadMigrationCorpus` normalises now; any future checksum must do the same.

## Commands

```bash
npm run typecheck                       # the ONLY real type gate
npm run lint
npx vitest run --config vitest.config.ts            # app + functions + claude; needs NO Docker
npx vitest run --config vitest.config.ts --project app
npx playwright install                  # only needed on a fresh clone; Chromium 1223 is already installed here
docker info                             # preflight: start Docker Desktop before any Supabase work
npm run test:db                         # RLS + tenant-isolation SQL suites; needs the isolated stack below
npm run test:db:engine                  # worker runtime + Company OS services through the real pg pool; locally prefix SUPABASE_DB_CONTAINER=supabase_db_atomic-crm-e2e SUPABASE_DB_PORT=54342
npm run test:db:upgrade                 # DESTRUCTIVE upgrade replay over legacy data (resets the e2e stack; copy new migrations into .supabase-e2e first); CI: -- --workdir .
npm run check:local-exposure            # exit 1 if any local Supabase port is reachable beyond loopback; 2 if it cannot tell
npm run worker:provision                # create ops_worker_login; needs OPS_WORKER_PASSWORD
npm run worker                          # run the worker; needs OPS_WORKER_DATABASE_URL (model routes: AGENT_MODEL_PROVIDER=openai, OPENAI_API_KEY, AGENT_MODEL_STANDARD — backend env only, never VITE_)
npm run ops -- status                   # operator view (ADR 0017), read-only by default: status | stops | routes | prices | limits | spend | runs | indeterminate | triage list | triage show; acts: price record | limit set | limit retire | triage accept | triage reject | triage needs-edit | triage recover; needs ADMIN_DATABASE_URL only
npm run execution-stop -- list          # kill switch (ADR 0010/0016/0017): trip --scope global|tenant|company|department|agent|job_kind [--kind <job kind>] --reason … --actor …, clear, list [--all]; needs ADMIN_DATABASE_URL
npm run agent-runtime:smoke             # local/manual only, never CI: one synthetic agent run on the fake provider; --live skips itself when no provider is configured
npm run lead-triage:demo                # local/manual only, never CI: the Phase 2A flow end to end on the fake provider; needs COMPANY_OS_SYNTHETIC_INGRESS=enabled, ADMIN_DATABASE_URL and OPS_WORKER_DATABASE_URL on the same local database; refuses while any job is queued or leased. No live mode: Q8 is open
npm run gateway:provision               # create ops_gateway_login (Phase 2B); needs ADMIN_DATABASE_URL, OPS_GATEWAY_PASSWORD
npm run whatsapp:gateway                # the Meta webhook gateway; needs OPS_GATEWAY_DATABASE_URL, WHATSAPP_APP_SECRET, WHATSAPP_VERIFY_TOKEN; binds 127.0.0.1:8787 by default (TLS belongs in front of it)
npm run messaging -- outbound list      # channels list | channels set (--mode production needs --inactive: the Q8 gate is closed) | send | outbound list | outbound show | outbound mark-indeterminate; needs ADMIN_DATABASE_URL; `send` (the ONLY way a message leaves) also needs WHATSAPP_ACCESS_TOKEN. No retry command exists, by design
npx supabase start --workdir .supabase-e2e   # ALWAYS use this workdir - see below
npx supabase db reset --workdir .supabase-e2e --local
npm run dev                             # against Supabase
npm run dev:demo                        # against FakeRest, no backend
```

Environment here: Windows 11, Node v22.23.1, npm 10.9.8, Docker **CLI** 29.7.2, Supabase CLI 2.117.0. `node_modules` (674 packages) and Playwright Chromium 1223 are installed and working. ⚠️ **The Docker *daemon* is not running** — the CLI version above says nothing about that; run `docker info` before any Supabase work. An earlier version of this line claimed Docker was working; that was checked against the CLI only.

## What NOT to do

- 🔴 **Never run `supabase start` / `db reset` without `--workdir .supabase-e2e`.** A second working copy at `D:\download\CRM` shares this repo's `project_id`, so the Docker volumes collide: a reset destroys *that* project's database, and a plain `start` silently attaches to it, making every verification measure the wrong schema. The `.supabase-e2e` config uses `project_id = atomic-crm-e2e` and ports 5434x.
- **Do not hand-edit anything in `supabase/migrations/`.** Reconcile via a new generated migration.
- **Do not run `supabase db diff` casually.** The next diff emits one migration mixing the clinic pipeline, the 43-policy rewrite, the grants and the storage change. Packaging it is Phase 0.5 work, not a side effect.
- **Do not reformat `supabase/schemas/02_functions.sql`.** Function bodies must match `npx supabase db dump --local --schema public` exactly or every future diff emits phantom changes.
- **Do not repurpose `public.companies` or `company_id`** — they mean *CRM customer account*, not tenant. Introduce a distinct name. *(2026-09-13: `ops.companies` and `ops.*.company_id` (ADR 0015) are the Company OS organisational entity, always schema-qualified. The tenant is `ops.tenants` / `tenant_id`, and a company is never a tenant or an isolation boundary.)*
- **Do not extend `public.tasks`** for engine work; `contact_id` is `not null` and it is a live CRM feature with e2e coverage.
- **Do not treat `activity_log` as an audit log.** It is a `UNION ALL` view over `created_at` columns that can only emit `<entity>.created`; a deleted row erases its own history.
- **Do not adopt `CrmDataProvider` as the CRM port.** It is `ReturnType<typeof getDataProviderWithCustomMethods>` — the Supabase implementation's own type — and both providers reach it only through an unsafe cast. Write the port by hand.
- **Do not refactor `.claude/`.** 30+ hooks depend on each other's `/tmp` state and git topology. Study it for patterns; leave it alone.
  - ⚠️ **One narrow exception, authorised by the owner on 2026-09-11: fixing the Windows path defects that make the `claude` test project red.** The rule's own rationale — hooks share `/tmp` state — is exactly what `sanitizePath()` governs, so this was checked before changing it: the new implementation is **byte-identical on every POSIX path** (verified against 6 representative paths) and only stops producing an *invalid* path on Windows, where nothing worked to begin with. The exception covers Windows-compat bugfixes only. It is **not** licence to restructure, rename or tidy anything under `.claude/`, and anything beyond a Windows defect still needs a fresh decision.
- ~~**Do not rewrite `supabase/functions/mcp/validateSql.ts`.** It is incomplete but it is the only guard between a model and `DROP TABLE`. Extend it and its tests. Two known holes to close first: it never inspects `stmt.in`, so a CTE attached to DML passes the read-only gate; and it classifies statement *types* only, so `SELECT extensions.http_get(…)` is accepted.~~ **Do not add a generic SQL endpoint back** *(2026-09-13)*. The MCP function and `validateSql.ts` are removed; `scripts/production-scope.mjs` refuses a function outside the reviewed allowlist, a known MCP server or SQL parser in function source, and a function deploy that names no reviewed function (SI-03). Agents get explicit, allowlisted operations through a future Tool Gateway.
- **Do not enable Supabase realtime publications or `[auth.oauth_server]`.** Each was deliberately closed. ⚠️ **The attachments bucket is a different case: it is not closed.** `07_storage.sql` *declares* it private, but that is DML, so no generated migration carries it and every migrated database still has `public = true` with three open `authenticated` policies (`20240730075029_init_db.sql:555-562`). Closing it needs a hand-written migration. Do not restore the upload path before that lands, and do not assume the declaration is in force.
- **Do not edit `docs/product/*.md`.** Tracked prior art in Portuguese (committed `652784b3`), kept as a dated record. Supersede with a note instead. (The reason is provenance — an earlier version of this line said "untracked, so an edit is unrecoverable", which was false.)
- **Do not modify `src/components/admin/` or `src/components/ui/`.** They are inbound shadcn-registry content (`registry.json` lists 214 `atomic-crm` files and **zero** from those directories). `AGENTS.md` says otherwise; `docs/product/07-upstream-strategy.md` wins — see [ADR 0008](docs/adr/0008-fork-posture.md).
- **Do not commit secrets.** `.gitignore:42` deliberately un-ignores `supabase/functions/.env`; an EC private signing key is already tracked in `supabase/signing_keys.json`. Never let a provider key follow that path.

## Conventions

- **English** for source, docs, agent prompts, config keys and default config values (`.claude/rules/english-only.md`). Tenant-language vocabulary belongs in data, not code.
- New engine code goes **outside** `src/components/` — every file added under `src/components/atomic-crm/` is republished publicly via `registry.json` on every push to `main`.
- Structured, schema-validated outputs for every agent. Never parse prose. ~~(Resolve the `zod` v4-app / v3-edge split before sharing schemas.)~~ *(2026-09-14: the v3 edge import went with the removed MCP function; the engine's output contracts use the app's zod v4, and no edge function imports zod. A schema shared with an edge function would reopen it.)*
- Every external action is idempotent. Every important action writes an audit row.
- Before a large change, read the relevant doc in `docs/`. After a large change, update it.
- Use targeted file inspection. Do not re-read the whole repository; that is what these documents are for.

---


# Agent Workflow

Code-change requests can run through the **agent harness**: subagents (planner, developer, quality-reviewer, merger, documentator) implementing the change via a deterministic foreground pipeline in git worktrees. **Opt-in, off by default;** otherwise the main thread implements the change itself.

**Dispatch rule (top-level session only).** Only on an opt-in (see "Opting in") does the top-level session dispatch the `orchestrator` and relay its result; never route or implement it yourself then. Pass your `<session_dir>` in the prompt (it namespaces worktrees/branches). A subagent NEVER dispatches an orchestrator (`block-nested-orchestrator` enforces it). The orchestrator owns all routing (SIMPLE/COMPLEX, plus SETUP/MEMORY/ROLLBACK-CONFLICT/RECOVERY, dispatch templates, waves, promotion, migration round) and drives the team to a terminal point. Each agent's last line is an output contract (`.claude/rules/agent-output-format.md`).

**"Launched" is NOT "done".** The dispatch is meant to block, but some runtimes (interactive Claude Code / the VS Code extension) return immediately with `Async agent launched … agentId: <id>` and deliver the result later as a `task-notification`. That ack means dispatched, not finished: do NOT fill the silence by implementing the feature yourself or re-dispatching (that duplicates the developer's work). While it runs, only surface progress, then relay the final report. On completion, before relaying, check `<session_dir>/needs-recovery` (written by `completion-invariant` when the orchestrator stopped with APPROVED-but-unmerged work): if present, dispatch a FRESH `orchestrator` with `<intent>recovery</intent>` and the same `<session_dir>` (never `SendMessage` the old one), wait, then relay.

**Resuming after a restart.** Closing the VS Code window / editor (or rebooting) between two harness steps kills the orchestrator's background process, but Claude Code reuses the SAME session id when you reopen the conversation, so the harness namespace (`<session_dir>`, `session/<short>` branch, worktrees) is unchanged. The gate pauses (plan gate, migration gate) are the exposed windows: the orchestrator has ended its turn and this thread is idle awaiting you, so a `task-notification` for the old (now dead) process will never arrive. Two things make resume work: (1) `cleanup-session` no longer deletes an in-flight session's state on a clean close, and (2) the `session-bootstrap` SessionStart hook detects in-flight state for the current session and injects a `<harness_resume>` banner naming the phase. When you see that banner (or the user asks to continue an interrupted harness), dispatch a FRESH `orchestrator` with `<intent>recovery</intent>` and the same `<session_dir>` (from the banner / the earlier turns), wait, then relay. NEVER `SendMessage` the previous orchestrator (it is dead) and never re-implement the work yourself. This is keyed on the current session id only, so a second concurrent window (its own session id) is never offered another session's work.

**PD-ASK round-trip (migration confirmation).** The orchestrator may end its turn asking *"apply the database migration now?"* (it does not under `gate=none`, which auto-applies; see "Gate level"). Relay it to the user; do NOT `SendMessage` the old orchestrator to relay the answer (relayed approvals carry no user authority, so it loops re-asking). On the reply:
- **Approved**: FIRST write `<session_dir>/migration-approval.json` = `{"kind":"migration-approval","session_id":"<id>","question":"<asked>","answer":"<user's verbatim reply>","approved_at":"<ISO-8601>","via":"AskUserQuestion"}` (the durable audit trail). THEN dispatch a FRESH `orchestrator` whose prompt begins `<intent>apply-migration</intent>`, states the approval, references that record, and passes the same `<session_dir>`. The built-in security warning on a relayed approval is expected.
- **Wants changes**: dispatch a fresh `orchestrator` with the new request.

While a fresh dispatch runs, don't start a parallel plan B; wait, then relay.

**Gate level.** A request may carry `gate=none|migration|plan|waves` (default **`plan`**); ALWAYS pass an explicit `GATE: <level>` line (the orchestrator also fails closed to `plan` on a missing/unknown value). There are two independent pause points: the **plan gate** (after planning) and the **migration gate** (before applying the deploy-time migration).
- `none`: fully autonomous. No plan pause, and it applies the migration itself with no confirmation.
- `migration`: pauses after planning for ticket review, and stops to confirm before the migration (same stops as `plan`; a named alias that states the migration stop explicitly).
- `plan` (default): pauses after planning for ticket review, and stops at the migration.
- `waves`: pauses after planning, after each wave, and at the migration.

On the web-chat surface (CRM Builder, `<mode>` tag present) the "save to your data?" confirmation is surface-owned and always shown regardless of gate; `none` auto-apply is a developer-surface behavior. On a plan pause, so the user reviews the REAL tickets (not just your prose summary) on ANY surface (desktop app, CLI, editor), **read the ticket JSONs from `<session_dir>/tickets/` and present each ticket's key fields inline** in the chat: id, title, `acceptance_criteria`, `files_to_modify`, `dependencies`. This inline rendering is the part that must ALWAYS be present (it does not depend on any editor layout). Additionally, offer an openable reference when you can: if a `.harness-session` symlink exists at the workspace root (the `link-session-workspace` hook maintains it in a VS Code multi-root workspace), link `[tickets](.harness-session/tickets/)`; otherwise just give the absolute `<session_dir>/tickets/` path. On approval dispatch a FRESH `orchestrator` with `<intent>execute-plan</intent>` and the same `<session_dir>` (never `SendMessage`); on "wants changes" dispatch a fresh one. CRM Builder's launcher sets `gate=none`.

## Opting in

Off by default. `#harness` (or "use the agent team" / "with the harness") routes through the orchestrator; "harness for this session" keeps it on all session.

**Grill vague requests first.** For a vague or broad `#harness` request, run `Skill({skill: "grill-me"})` in the main thread BEFORE dispatching (the planner never questions; `gate=plan` only reacts after a plan). Fold the answers into the dispatch prompt. Skip when the request is already precise.

`#technical-harness` is the same opt-in for a real developer: it appends a `PERSONA: technical` line (see `orchestrator.md` -> "`PERSONA: technical`"), which (1) uses the full technical register (file paths, `TASK-XXX`, git terms, `database`/`migration`/`Supabase`), (2) reports the mechanical truth (per-ticket status, branches, SHAs, verdicts, ADR paths), (3) stops at `session/<id>` (no promotion, no migration round; you promote and migrate yourself), and (4) appends every step to `<session_dir>/harness-progress.log`.

**Live progress (technical runs).** The report lands only at the end (possibly as a `task-notification`), so surface a live view WITHOUT flooding the chat with a raw log tail. The `render-status` SubagentStop hook renders `.harness/<SESSION_SHORT_ID>/{STATUS.md,TICKETS.md,status.json,session.diff}` (gitignored, per-session, cleaned at SessionEnd) from the live session state after every agent stop. **It does not exist until the first agent stops**, so during initial planning there is no board yet: do NOT present its path at dispatch (a link then is dead: "Couldn't read this file"). Relay it only from the plan-gate pause onward, once the files exist; at dispatch, just say the board will be available once planning completes. `STATUS.md` lists tickets (status + review verdict), live worktrees, a changed-files summary, and recent activity, updating itself as agents finish; `TICKETS.md` is the readable plan; `session.diff` is the actual code the harness changed (the extension does not surface the `/tmp` worktree diffs, so this file is how the user reads them). **Give a path that resolves in the user's client - always the ABSOLUTE path.** The board lives at `<repo>/.harness/<SESSION_SHORT_ID>/`. A bare relative `.harness/...` fails with "outside the working directory" on both the desktop app and a multi-root VS Code workspace, because the client resolves it from the workspace root, not the repo subfolder. So ALWAYS give the absolute path (`<repo>/.harness/<SESSION_SHORT_ID>/STATUS.md`): the desktop app's file pane opens arbitrary absolute paths, and it is the universal fallback. Markdown relative links to `.harness/...` (and to the `.harness-session/` symlink) are unreliable in the VS Code extension - observed: clicking does nothing - so do NOT rely on them. Give the ABSOLUTE path (the desktop file pane opens it, and it copy-pastes cleanly everywhere), and to put openable ticket CONTENT in the chat, `Read` the JSON: a `Read` renders a clickable file card that actually opens, which the plan-gate inline render already does. An opt-in compact statusline is available (`.claude/scripts/harness-statusline.mjs`; wire it in your own `settings.local.json`, see its header) - not committed, since it would override a personal statusline. It is **CLI-only** (the terminal statusline); it does NOT render in the VS Code extension or the desktop app, so on those surfaces use the board or the Monitor feed below. Dispatch the orchestrator with `run_in_background: true`. Prefer the board; for a live in-chat feed on ANY surface (extension / desktop / CLI), a `Monitor({command: "tail -n +1 -F <session_dir>/harness-progress.log", ...})` streams each NEW log line as a chat notification the moment it is written - now including the `[validate:TASK-XXX] typecheck…` / `… checks passed` step lines the `validate-on-stop` hook appends, so the minutes-long silent stretches between milestones (a developer's typecheck / lint / vitest / e2e) are visible instead of going dark. It is a line feed, not a rendered board, and is auto-rate-limited if the log is too chatty, so keep the log at milestone/step granularity. The authoritative end signal is the `task-notification`, not the log: on it, `TaskStop` any monitor, relay the report, and run `/harness-diff` on the session branch it stopped on.

## Agents

orchestrator (routes the harness, dispatched by the main thread), planner, developer, quality-reviewer, merger, documentator. Models: **planner** and **quality-reviewer** on opus, the rest on sonnet/haiku (see each `.claude/agents/*.md`). The web-chat variant is this orchestrator with a non-technical persona injected via `--append-system-prompt` (CRM Builder).

The **developer** is one agent, no modes: it implements a `TICKET_FILE` (COMPLEX wave, peer-reviewed, ADRs for structural decisions, never SQL during tickets) or an inline `CHANGE_REQUEST` (SIMPLE, on the shared `<base>/simple` worktree; refuses `FAILED: out of scope, needs COMPLEX flow` if it needs a breakdown). Two session ops reach it as dispatch-loaded skills on `<base>/simple`: `writing-migrations` and `resolving-rollback-conflicts`. It applies the **Ponytail** ladder (full mode) on every change (baked into `developer.md`; the quality-reviewer enforces it on the diff). Ponytail is also in-repo as `.claude/skills/ponytail*` / `/ponytail*` for interactive use (these do not affect the dev agents). An optional `test-writer` runs only when a ticket sets `separate_test_writer: true`.

## Rules & hooks

Mechanics live in `.claude/rules/` (worktree-scope, agent-output-format, validation-commands, lsp-usage, security-triggers, dependency-safety, launcher-interface). Project facts (validation steps, roles, deploy adapter, app smoke, launcher extension points) live in `harness.config.json`. Hooks in `.claude/settings.json` / `.claude/hooks/` are `.mjs` ES modules.
