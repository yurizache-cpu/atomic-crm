# Architecture Decision Records

Index of ADRs under [`docs/adr/`](adr/). Each records context, decision, alternatives and consequences, so that a future session does not casually reverse a deliberate choice.

**Reconciled 2026-09-11 (Phase 0.5, Workstream K).** Every record was re-read against the implementation. One contained a materially false security claim and has been corrected; three decisions the owner made in the Phase 0.5 brief are now `Accepted`; the rest remain `Proposed` and still await approval. **Update 2026-09-13:** the owner has since accepted 0012 (2026-09-12) and 0015 (2026-09-13), each with an addendum. 0002 and 0003 were reconciled with both on 2026-09-13 and remain `Proposed`; see their rows. **Later 2026-09-13:** the owner accepted 0003 as reconciled; 0002 stays Proposed (see its row). **Final 2026-09-13:** the owner accepted 0002 under the final pre-1D acceptance bar (SI-27; ~~local evidence, CI pending~~ CI verified 2026-09-14, run 34836911824 on `a1f9bbca`). **2026-09-16/17:** the owner accepted 0016 and 0017 after owner review; see their rows. **2026-09-21:** the owner accepted 0018 with amendment (three owner amendments); see its row. **2026-09-22:** after reviewing the Phase 2C brief, the owner accepted 0019 for implementation (not implemented) and approved a Phase 2C addendum to each of 0004 and 0010; both of those records stay `Proposed` as a whole; see their rows and decision P below. **2026-09-22 (final confirmation):** the owner accepted OD-8a as a strict, pinned guard exception, keyed Company OS membership to the auth user id (never an email), approved narrow amendments of SI-15, SI-21, SI-39 and SI-52 in principle (their proposed exact texts were recorded in the brief §15 for the owner's review before push), and recorded the residual-trust decisions; see the Phase 2C final owner confirmation below. **2026-09-22 (pre-push review):** the owner corrected the exact texts of SI-21 and SI-39, limited OD-8a to exactly pinned, self-contained migrations with no standing exception, and recorded that a browser review decision decides the structured review, never the hidden reply draft or a send; see the Phase 2C owner pre-push review below. **2026-09-22 (final):** the owner approved SI-39's print clause, the last pending Phase 2C decision; every Phase 2C owner decision is now final.

> 🔴 **Approving the remainder is a scheduled task, not a formality.** ADRs 0001 and 0002 carry *very high* reversal cost and are implemented in Phases 2 and 3. *(2026-09-13: both are already implemented in part, by Phases 1A–1C, while still Proposed. Their review is overdue, not upcoming.)* Building on an unapproved ADR converts a reversible choice into an accident.

| # | Decision | Status | Reversal cost |
| --- | --- | --- | --- |
| [0001](adr/0001-runtime-execution-substrate.md) | Runtime execution substrate — Postgres queue + always-on worker | Proposed | **Very high** — rewrites every tool call |
| [0002](adr/0002-tenancy-model.md) | Tenancy — tenant is the only isolation boundary; `tenant_id` + FORCE RLS on tenant-data `ops` tables, tenant from a live lease, privilege and composite-key isolation for the Company OS; `ops` off the Data API | **Accepted 2026-09-13** under the owner's final pre-1D acceptance bar ⚠️ **corrected** 2026-09-11. The MCP channel is closed by removal and kept out (SI-03), hosted functions are checked before every deploy, and the `merge_contacts` owner-session pool is measured unable to reach `ops` or carry state (SI-27). ~~Local evidence; CI pending~~ CI verified 2026-09-14 (run 34836911824 on `a1f9bbca`) | **Very high** — retrofitting tenancy touches everything |
| [0003](adr/0003-identifier-strategy.md) | Server-generated UUID keys on engine entity tables; no FK into `public.*`; tenant-scoped CRM references; key-based idempotency | **Accepted 2026-09-13** — as reconciled; global monotonic identifiers (`events.seq`, `job_events.id`, any future sequence) are internal only | High — one bigint FK welds the CRM to the core |
| [0004](adr/0004-principal-model.md) | Engine-owned `principals` (human / agent / service) | Proposed — the record as a whole. Owner-approved addendum 2026-09-22 (Phase 2C, OD-3 and the final confirmation): `ops.principals` for humans, keyed to the auth user id (never an email; the email at grant is detection only), and an explicit, owner-provisioned, human-only, tenant-generic `ops.tenant_memberships`, never derived from CRM owner or administrator state; Phase 2C eligibility is limited to the tenant that owns the local CRM as a temporary policy; approved for implementation, not implemented | High — determines whether agent actions are auditable at all |
| [0005](adr/0005-ra-core-boundary.md) | `ra-core` never crosses into the engine | Proposed. Line 25 (Phase-1 ops screens using `ra-core` against a worker API) is superseded for the operator surface by 0019 (2026-09-22) | High — unwinding `RaRecord` later is a full rewrite |
| [0006](adr/0006-declarative-schema-workflow.md) | Make the declarative schema workflow actually wired | Proposed | Low to fix now, **catastrophic to leave** |
| [0007](adr/0007-launcher-relationship.md) | Company OS supersedes the chat-service launcher; its config is inert dev tooling | **Accepted** | Low — re-integrating later is additive |
| [0008](adr/0008-fork-posture.md) | Hard-fork; stop publishing the registry | Proposed | Medium — cherry-picking upstream fixes becomes manual |
| [0009](adr/0009-governance-envelope.md) | Governance envelope — risk as data, fail-closed default, confidence escalates but never skips review | Proposed | High — a gate retrofitted after agents act is a gate nobody trusts |
| [0010](adr/0010-cost-control-and-kill-switch.md) | Cost ledger in-transaction, per-agent budgets, **fleet-wide kill switch** (scopes + semantics added in Phase 0.5) | Proposed — the record as a whole. Owner-approved addendum 2026-09-22 (Phase 2C, OD-6) amending the UI half of Decision 2(c): a Company OS member may trip a tenant, company, department or agent stop, with a required reason, through `ops.trip_execution_stop` under its lock, never clear one and never trip a global, system or `job_kind` stop; the fleet-wide trip stays one action in the CLI; deny wins, serialisation with run admission and manual clearing unchanged; a bounded request with a generic, retryable contention failure (final confirmation); approved for implementation, not implemented | Low to build now, **unbounded to omit** |
| [0011](adr/0011-mcp-trust-boundary.md) | The MCP function is **not** part of the production trust boundary; no agent gets arbitrary SQL. **Removed 2026-09-13** and kept out of the committed deploy paths (SI-03) | **Accepted** (owner, Q12; removal addendum 2026-09-13) | Low — it constrains a component rather than shaping the engine |
| [0012](adr/0012-worker-tenant-context.md) | Worker tenancy — scoped non-superuser role, tenant resolved from a **live lease** | **Accepted 2026-09-12** — with the Phase 1A addendum; the original pure-GUC design is superseded | **Very high** — it is the mechanism ADR 0002 depends on |
| [0013](adr/0013-pipeline-stages-are-configuration.md) | Pipeline stages are tenant configuration, not schema semantics | **Accepted** (owner, Q4) | Low **now**, high once migrated — which is why it was done before the first migration |
| [0014](adr/0014-inbound-email-ledger-keys-on-recipient-email.md) | The inbound-email ledger keys on `(message_id, recipient_email)`, not `recipient_contact_id` | **Accepted** | Low — one table, one unique index |
| [0015](adr/0015-company-os-domain-core.md) | Company OS domain core — companies, departments, agents, tasks and events in `ops`; composite-key integrity; backend-only SECURITY INVOKER services; lifecycle events derived by triggers; a task→job bridge with an empty allowlist | **Accepted 2026-09-13** — with the owner addendum: tenant is the only isolation boundary, businesses needing independent data isolation are separate tenants, global monotonic identifiers (`events.seq`, `job_events.id`, any future sequence) internal only, minimised event payloads, neither `postgres` (the database owner) nor `service_role` is ever an ordinary agent or worker identity | High — the organisational model every agent will operate on |
| [0016](adr/0016-agent-runs-and-model-providers.md) | Agent runs — one bounded model call per run, at most once (durable start before the call, `indeterminate` never retried, a retry is a new run); explicit task-scoped request through the bridge; idempotency, correlation and causation as distinct identifiers; lease-bound worker capabilities; structured advisory output validated twice; a provider-neutral `ModelProvider` with one fetch-based OpenAI adapter and a test-only fake; a deterministic tier router; usage without invented cost; the minimal agent-run kill switch | Accepted (2026-09-16, owner review: an outcome is `failed` only when the provider was never called or its answer settles it, so ambiguous outcomes, a 5xx included, are `indeterminate`; SDK rationale corrected. Proposed 2026-09-14, Phase 1D. CI verified 2026-09-16, run 35129049291 on `c4382b9b`) | High — every later agent capability, provider and tool call inherits these semantics |
| [0017](adr/0017-runtime-governance.md) | Runtime governance — versioned owner-recorded model prices (never in migrations, never stale, no fallback); cost derived by the database from usage and the price version recorded at start, an unknown outcome charged at its reservation; versioned daily spend limits (a global ceiling and a tenant budget that must exist, and an optional company limit; every configured limit must absorb a run's worst-case reservation), checked under per-scope locks; a global ceiling exhausted by settled spend trips the one kill switch as a system stop that only a person clears; the same switch holds every non-internal job at the lease without consuming an attempt, can name one external kind, and is re-checked by the runtime before any external call, and a stop an agent run's start finds holds the run and defers its job instead of cancelling it; idempotent `create_task` and `record_event`; an operator CLI, read-only by default | Accepted (2026-09-17, owner review: fail closed without price, ceiling or budget; stopped jobs held or deferred, never cancelled; a bounded wait for a prepare in progress; the spend-ceiling stop is cleared only by a person; a request made under a stop is refused before any job exists. Proposed 2026-09-17, Phase 1D.1. CI verified 2026-09-17, run 35220094426 on `b4dee040`) | Medium — every later integration and budget inherits these semantics |
| [0018](adr/0018-whatsapp-transport-and-human-send.md) | The WhatsApp transport — the official Meta Cloud API behind our own ports; a dedicated internet-facing gateway process and NOLOGIN role (`ops_gateway`: no table, exactly two target-bound DEFINER functions) that verifies the signature over the raw bytes before parsing; the tenant chosen only by an owner-configured provider target; BASELINE Q8 enforced on the channel (`test` or `production`, owner configuration; a closed real-data gate with no enabled value, so a production channel can exist only inactive; database triggers behind it); a message acknowledged only once admitted or refused on the record, an unrouted one answered non-2xx and stored nowhere; conversations group messages while each message stays its own task; a send only by a second, explicit operator act with its send preconditions checked afresh (an active test channel, no covering execution stop, exactly one CRM contact, `do_not_contact = false`, and an inbound message inside Meta's 24-hour service window: preconditions, not affirmative consent or a lawful basis), at most once, ambiguity `indeterminate`, never resent; tenant-bound status reconciliation | Accepted with amendment (owner decision 2026-09-21: (1) opening the production real-data gate requires a NEW, explicitly owner-approved, Accepted ADR, never a migration alone, and that ADR must settle Q8, the lawful basis, the representation of real WhatsApp consent and opt-in, and the retention and erasure of message bodies and phone identifiers; (2) before any production WhatsApp enablement, a live probe in Meta's official test/development environment is mandatory (a Meta test/development number, a controlled test recipient, synthetic content only, no real patient traffic), verifying send request compatibility, the selected Graph API version, the provider message id, the status callback format, webhook signature behaviour and the actual placement and behaviour of `biz_opaque_callback_data`, which stays UNVERIFIED until then; (3) ADR 0018 does NOT approve the pre-existing PUBLIC EXECUTE surface of the CRM row-level-security helpers: it remains separate security debt, no additional PUBLIC helper may appear, and the existing grants need their own explicit review and decision before real production patient traffic. Proposed 2026-09-18, Phase 2B; amended by the focused pre-push review the same day, before any push) | Medium — every later channel and outbound action inherits the gateway role, the channel gate and the send semantics |
| [0019](adr/0019-company-os-operator-surface.md) | The Company OS operator surface — an in-app, ra-core-free `src/company-os/` module; a function-only PostgREST RPC schema, `company_os_api`, whose functions are owned by the NOLOGIN least-privilege role `ops_operator_api` (USAGE on `ops`, EXECUTE only on one identity gate per operation, no table privilege); an explicit owner-provisioned human membership keyed to the auth user id (never an email) as the only tenant source, tenant-generic, with a temporary Phase 2C eligibility policy limiting it to the tenant that owns the local CRM; exactly two browser acts (a review decision, where accepting sends nothing, and a trip-only stop at tenant, company, department or agent scope); minimised tenant-scoped projections with one derived platform boolean; at most 17 functions; local and test only; `ops` never exposed | **Accepted 2026-09-22** for implementation (owner review of [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md), OD-1 to OD-15, then the owner's final confirmation: OD-8a as a strict pinned guard exception for the PostgreSQL 15 temporary role membership and temporary CREATE that give the functions to `ops_operator_api`; the narrow amendments of SI-15, SI-21, SI-39 and SI-52, approved in principle with proposed exact texts, now final; the residual-trust decisions; then the owner's pre-push review: SI-21 and SI-39 corrected, OD-8a limited to exactly pinned, self-contained migrations with no standing exception, and a browser review decision recorded as deciding the structured review, never the hidden reply draft or a send). **Final status:** SI-15, SI-21, SI-39 and SI-52 are owner-approved final texts (brief §15, ADR 0019 Decision 9); SI-39's `triage show` print clause is OWNER-APPROVED; every Phase 2C owner architecture decision is final; the PR #6 architecture review (2026-09-22) removed raw actor labels from the browser projections and tightened the `ALTER DEFAULT PRIVILEGES` guard. **Architecture integrated** into `feature/clinical-phase-1` by PR #6 on 2026-09-22 (normal merge commit `fe7976c5`, reviewed head `95bf2faa`, branch retained, `main` unchanged); post-merge run 35767741221 concluded failure with only the accepted baseline reds. **Not implemented.** Implementation waits for planning spikes S0.1 to S0.6; the two browser mutations wait for the user-management prerequisite. Amends 0004 and 0010 by owner-approved addenda; supersedes 0005's line 25 for the operator surface. Q8 stays OPEN and the production WhatsApp gate CLOSED | Low to medium — remove the schema from the exposed list to turn the UI off; revert one `src/App.tsx` switch; the tables are additive |

### What changed in the reconciliation

- **0002 carried a false claim** and it was load-bearing: *"The engine is unreachable from any browser by construction."* The PostgREST allowlist governs one channel; the MCP function holds a direct libpq ~~**superuser**~~ `postgres` connection *(corrected 2026-09-13: `rolsuper=false`, `rolbypassrls=true`)* that ignores it. The claim is retracted in place, narrowed to "unreachable *through PostgREST*", and the second channel is now governed by 0011. Its RLS-helper guidance was also wrong for the engine (`auth.uid()`-based, and the worker has no JWT) and now points at 0012. **0002 cannot be accepted until 0012 is.** *(2026-09-13: 0012 is accepted, so that precondition is met. 0002 now waits on the MCP channel: ADR 0011 item 4 discharged, and a test through that channel; see its 2026-09-13 addendum.)* *(Later 2026-09-13: the MCP channel is closed; 0002 now waits on a test of the `merge_contacts` owner-session pool and on CI.)* *(Final 2026-09-13: the owner-session pool is measured unable to reach `ops`, and 0002 is accepted.)* *(CI verified 2026-09-14: run 34836911824 on `a1f9bbca`.)*
- **Nothing was marked `Accepted` on architectural merit alone.** 0011 and 0013 are `Accepted` because the owner decided them verbatim in the Phase 0.5 brief (Q12, Q4); 0007 was delegated earlier. Everything else stays `Proposed`.
- **0012 is `Accepted` as of 2026-09-12**, and what was accepted matters: the owner accepted the **Phase 1A addendum**, not the ADR as originally written. Tenancy is bound to a live lease, not to a GUC the worker writes — measured, a worker can set any GUC, so the original item 4 would have made "which tenant am I" an assertion by the worker. A Phase 1B addendum then corrected the transaction boundary: the lease commits on its own, because a single-transaction lease leaves a crashed worker no trace and makes a crashing job a poison pill.
- **No ADR is `Superseded` or `Rejected`.** Recorded explicitly so the absence reads as a finding rather than an oversight.

### Owner decisions recorded outside an ADR

Phase-level owner decisions that settle behaviour without a new architectural record. Each is recorded in full in the phase report it names.

- **F — Legacy administrators are never promoted to owner automatically** (2026-09-17, [PHASE_1D2_REPORT.md](PHASE_1D2_REPORT.md) §16). The historical `administrator` flag is not ownership evidence. An upgrade that meets legacy administrators without an active owner halts until a person runs `public.bootstrap_owner`, then resumes; the other legacy administrators become recorded operators. Runbook: [PERMISSIONS.md](PERMISSIONS.md) §3.
- **G — A representation repair keeps the business `updated_at`** (2026-09-17, same report). The legacy deal-stage backfill must not manufacture an interaction timestamp.
- **H — A backfilled lead profile's `acquired_at` is derived from the contact's earliest trustworthy evidence** (2026-09-17, same report), never from the migration time when evidence exists; it never overwrites an existing profile and is documented as derived.
- **I — Open-source-first, adapter-first** (2026-09-17, [ARCHITECTURE_ACCELERATION_REVIEW.md](ARCHITECTURE_ACCELERATION_REVIEW.md) §14). A mature capability that exists as genuinely free, self-hostable open source is integrated behind one of our adapters; everything else is built with Claude Code. Source-available, fair-code and Enterprise-gated software does not qualify as acceptable open source, and "on GitHub" is not a licence.
- **J — WhatsApp runs on the official Meta Cloud API, behind our own `CommunicationPort`** (2026-09-17, same review). Paid and not open source, chosen deliberately: every OSS gateway reaches WhatsApp through unofficial WhatsApp Web access, and a banned number is a patient-channel outage. A cost and data-processing decision, separate from Q8; the adapter is ours to build and the message ledger stays in our database.
- **K — cal.diy is reference only** (2026-09-17, same review), despite the MIT relicensing of the former `calcom/cal.com`. Revisited only if scheduling grows beyond one clinic.
- **L — AGPL posture** (2026-09-17, same review). Grafana is allowed only as an internal, unmodified, self-hosted viewer if it is ever needed; Twenty is reference only. No AGPL/GPL/LGPL code is incorporated into proprietary Company OS source without explicit owner review of that specific dependency and use (review §15.1).
- **M — An omnichannel inbox is not a Phase 2 goal** (2026-09-17, same review). Chatwoot and any external patient conversation store stay deferred; conversation state stays authoritative in our database.
- **N — Observability lands in Phase 2D** (2026-09-17, same review), after the first real flow exists, rather than before there is anything to observe. *(Superseded for sequencing by decision P, 2026-09-22: observability moves from Phase 2D to Phase 2E. The observability requirement itself is unchanged, and this entry stays as the record of the former sequence.)*
- **O — Q8 remains open** (2026-09-17, same review). The review assumed it and changes nothing about it.
- **P — The sequence after Phase 2B** (2026-09-22, owner review of [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md) §23). **Former sequencing:** the 2026-09-17 revised sequence (ROADMAP; the architecture acceleration review) was 2B WhatsApp → 2C operator surface → 2D observability (decision N) → 3A scheduling and follow-up → 3B RAG → 3C growth. **New sequence:** 2C operator surface; 2D decision engine and Jev; 2E observability and intelligence; 3A scheduling and follow-up; 3B memory and RAG; 3C growth. **What changed:** a decision-engine phase (2D, decision engine and Jev) is inserted before observability, which moves to 2E and widens to observability and intelligence. **Why:** the owner directed this order on 2026-09-21 and approved it on 2026-09-22. The owner's 2E scope (decision quality, Jev calibration, escalation metrics) measures the decision layer, which exists only after 2D. No further rationale was recorded. **Scope of the change:** sequencing only. Observability is still required, and the acceleration review's integrate-soon choice of OpenTelemetry and Prometheus (under decision I) is unchanged; nothing is added to Phase 2C for it. **Phase 2D direction, recorded, not implemented:** a provider-neutral `DecisionPort` with `JevDecisionProvider`, `RulesDecisionProvider` and `FakeDecisionProvider`, producing a `DecisionVector` (likely route, task type, complexity, risk signal or classification metadata, urgency, ambiguity, required capabilities, and a confidence or probability distribution). Jev is never authoritative for permission, the tenant boundary, budgets, the kill switch, lawful basis, consent, risk-policy enforcement or irreversible actions, and enters in shadow mode first, measured against actual routing outcomes, human decisions, model outcomes, latency, cost, calibration and escalation quality. No Jev code, dependency, variable, schema or migration exists. No real patient data reaches it until every prerequisite for real patient traffic is met (Q8 decided, the Phase 2B owner decisions, the Accepted ADR that ADR 0018 amendment 1 requires, the live Meta probe, the decision on the PUBLIC helper grants); Jev's Q8 impact is assessed in the Phase 2D brief and presented for the owner's explicit decision; Q8 stays an owner decision, and OPEN.
- **Phase 2C owner decisions OD-1 to OD-15** (2026-09-22, [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md) §21, [ADR 0019](adr/0019-company-os-operator-surface.md)). Phase 2C is approved as an operator surface over real durable state only. **OD-1** ACCEPTED: in-app, ra-core-free `src/company-os/`; a separate Vite entry only if spike S0.1 proves it necessary. **OD-2** ACCEPTED: a function-only `company_os_api` PostgREST RPC schema; no CRUD, arbitrary SQL, table exposure or generic `ops` proxy; `ops` unexposed; every RPC pinned; no new origin or process. **OD-3** ACCEPTED: explicit Company OS membership, never inferred from CRM owner or administrator state or from browser-supplied ids (ADR 0004 addendum); in Phase 2C only for the tenant that owns the local CRM. **OD-4** ACCEPTED: only two browser acts, a review decision and a stop trip. **OD-5** ACCEPTED WITH LIMIT: review advice only on explicit open, synthetic or test only, through a capability-pinned minimised projection, never in durable browser storage, no generic content viewer; acceptance never sends. **OD-6** ACCEPTED WITH AMENDMENT: trip only, at tenant, company, department or agent scope, through the authoritative function under its lock, with a required reason; no clear, no global or system stop (ADR 0010 addendum, amending the UI half of its Decision 2(c)). **OD-7** ACCEPTED: tenant-scoped information only; the one platform field is the derived boolean `globalAdmissionBlocked` (the owner's example); no global ceiling amount or global spend. **OD-8** OWNER AMENDMENT: a dedicated NOLOGIN least-privilege role owns the exposed functions and may execute only the per-operation identity gates; the boundary is the pinned function graph. **OD-8a** PENDING owner confirmation: the narrow static-guard extension that the PostgreSQL 15 ownership transfer requires (one literal temporary membership pair and one literal temporary CREATE pair; every other membership statement stays non-overridable). The owner is also asked to confirm the amended texts of SI-15, SI-21, SI-39 and SI-52 and the recorded residual trust (brief §24). *(OD-8a and the residual trust were settled the same day by the Phase 2C final owner confirmation below; the four SI amendments were approved there in principle, and their exact texts were settled by the Phase 2C owner pre-push review below, which corrected SI-21 and SI-39, and the owner then approved SI-39's print clause, the last pending decision.)* **OD-9** ACCEPTED: eight core screens; Communications optional, as a status view of counts only. **OD-10** ACCEPTED: MFA and CSP are not prerequisites for synthetic work; no bundle secret, no durable content storage, `no-store`, safe expiry, explicit sign-out, no analytics receiving content; the production gates remain. **OD-11** ACCEPTED: the `patchUser` / `createSale` debt is fixed before any mutable UI ships; read-only first. **OD-12** ACCEPTED: local and test first; hosted exposure needs its own readiness step; no production deployment approved. **OD-13** DEFERRED: notifications. **OD-14** ACCEPTED: ADR 0019, the ADR 0004 and ADR 0010 addenda, this file and decision P. **OD-15** ACCEPTED: no new dependency, process, origin or public generic API.
- **Phase 2C final owner confirmation** (2026-09-22, [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md) §21 and §15, [ADR 0019](adr/0019-company-os-operator-surface.md) "Owner confirmation record"). It settles what the review above left pending (OD-8a) and corrects two points of the same-day design text (the membership identity and the tenant eligibility); following the SI-52 amendment, it also keeps the reply draft out of the browser entirely and the classification enums out of everything but the explicit-open advice. It reverses no owner decision. **OD-8a** ACCEPTED WITH STRICT PINNED GUARD EXCEPTION: one role (`ops_operator_api`: NOLOGIN, NOSUPERUSER, NOCREATEDB, NOCREATEROLE, NOBYPASSRLS, NOINHERIT, no members at rest, no table or sequence privilege beyond the catalogue reads PUBLIC gives every role, no generic write authority, no way for the browser, PostgREST or a runtime login other than a superuser or CREATEROLE role, the database-owner trust root, to become it), one schema (`company_os_api`), the exact RPC catalogue and the exact Phase 2C migrations; a temporary role membership granted only to the repository's proven migration identity (measured during implementation, never assumed) and revoked in the same migration; a temporary CREATE on `company_os_api` only, never on `ops` or `public`, revoked in the same migration; `ALTER FUNCTION … OWNER TO` only to `ops_operator_api`, only for catalogued `company_os_api` functions, only in allowlisted migrations that carry the cleanup and the post-migration proof; every other role-to-role grant involving the scrutinised Company OS roles still refused, and every other function ownership change or ownership path naming `ops_operator_api` refused as a new non-overridable finding. No broad exception. **Membership identity:** the auth user id (`auth.uid()`), never an email; same email ≠ same principal; `email_at_grant` is detection only, never an authorization boundary. **CRM-admin account capture:** accepted only for local, synthetic and test Phase 2C; real data and any hosted operator surface are blocked until the `users`/`patchUser`/`createSale` authority and ordering issues are fixed and tested, so that no CRM administrator, the active CRM owner included, can retarget or acquire another person's Company OS principal. **Tenant eligibility:** the local CRM-owning tenant only, for Phase 2C, as a temporary policy; the core membership model stays tenant-generic, and lifting the policy needs an owner decision that separates that tenant's Company OS identity from the clinic's CRM trust model. **Token theft:** a known residual, accepted for synthetic and test development only; membership limits what a valid principal may access and does not prove a token was not stolen; the session-hardening, MFA, CSP, hosted-review and auth-hardening gates remain before real use. **Kill-switch contention:** the serialised behaviour is accepted; the browser trip uses the authoritative service with no pre-check, preserves deny-wins, serialisation, audit and origin, and must answer contention with a bounded, generic, retryable failure, never showing a stop before commit; driver-backed tests exercise contention. **SI-15, SI-21, SI-39, SI-52:** narrow amendments approved in principle; the proposed exact texts are recorded in the brief §15 and ADR 0019 for the owner's review before push, then written verbatim when implementation starts (SI-21's text names the reads and the review decision as well as the trip, a reconciliation flagged for that review and settled by the pre-push review below); following the SI-52 instruction, the browser advice excludes the reply draft, so a browser acceptance never shows the draft a later CLI send transmits (a residual recorded for the owner, not decided). **Mutable UI:** the two browser mutations stay disabled at the backend (their functions do not exist) until the user-management prerequisite is closed and tested; a frontend flag is never the boundary. Q8 stays OPEN, the production WhatsApp gate CLOSED, the Meta live-probe requirement and the PUBLIC helper debt unchanged; no real patient data and no production deployment are approved.
- **Phase 2C owner pre-push review of the exact texts** (2026-09-22, [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md) §21, §15, §7.6 A and §7.5, [ADR 0019](adr/0019-company-os-operator-surface.md) "Owner confirmation record"). The last governance correction before push; it reverses no owner decision. **SI-21 clarified:** direct Company OS authority (a Company OS table privilege, generic `ops` access, EXECUTE on a SECURITY INVOKER service) is denied to every application role and to the capability role `ops_operator_api`; a pinned SECURITY DEFINER capability is allowed only as explicitly catalogued; `ops_operator_api` is a NOLOGIN capability role, not a principal, executing exactly one gate per catalogued operation; `authenticated` executes only the catalogued `company_os_api` functions and never becomes it; `ops_worker` and `ops_gateway` keep their existing pinned sets; no `ops` function is executable by PUBLIC and no generic DEFINER access exists; the tenant-scoped reads and the two future browser acts stay, and clearing a stop stays an owner CLI act. **SI-39 reconciled with the real CLI:** the current text stays the record of the state before Phase 2C; the amended text makes the acts an explicit allowlist (price record, limit set, limit retire, triage accept, reject and needs-edit, triage recover, and from Phase 2C membership grant and revoke), every other command a read, no further act without a reviewed extension, and membership commands that have no option taking an email and never print an email, an email hash, an auth token or privileged connection information; `triage recover` opens missing reviews and is an act; the amended text's print clause also names the one existing exception, `triage show` printing the stored proposal, reply draft included, of the review item it names (true since Phase 2A; reconciled alongside the allowlist so the verbatim text is true; OWNER-APPROVED on 2026-09-22 as existing owner-CLI behaviour, not new authority; nothing newly permitted; SI-52's existing sentences unchanged). **OD-8a tightened, still ACCEPTED WITH STRICT PINNED GUARD EXCEPTION:** each migration that transfers ownership is allowlisted by its exact file name and itself runs the whole lifecycle (identity assertion, membership grant, CREATE on `company_os_api` only if required, only its pinned catalogued ownership operations, CREATE revoke, membership revoke, post-migration proof); the two Phase 2C migrations are acceptable only because each does so independently; there is no generic future exemption for ownership changes, `CREATE OR REPLACE FUNCTION`, `DROP FUNCTION`, EXECUTE grants or revokes, `ALTER DEFAULT PRIVILEGES`, function ACL changes or role membership, and a later catalogue or privilege-graph change needs a new forward migration, an explicit static-guard update, reviewed tests and owner or governance review where a capability changes. **Review acceptance and the hidden draft:** the draft's exclusion from the browser is ACCEPTED; a browser decision decides the structured triage review, never the hidden reply draft, authorises no WhatsApp send (REVIEW ACCEPTANCE ≠ SEND) and is never presented in UI or audit text as a message approved for sending; the send stays a separate, deliberate operator act, and reading the draft stays in the owner CLI path unless a future explicit decision creates another safe surface; that nothing enforces reading it before a send stays recorded, not decided. **Implementation consistency item:** the operator CLI's comments and tests and the canonical SI-39 still describe the pre-Phase-2A act set ("three narrow governance acts"); Phase 2C's implementation reconciles them when SI-39 changes, and this is not a reason to redesign Phase 2C. Every other owner decision is unchanged: the auth user id as membership authority, email as detection only, the temporary CRM-owning-tenant eligibility over a tenant-generic model, token theft and CRM-admin capture accepted only for synthetic and test work, bounded kill-switch contention with no optimistic success, Q8 OPEN, the production WhatsApp gate CLOSED, the live Meta probe mandatory, the PUBLIC helper debt open, no real patient data, no production deployment, Jev not implemented, and Phase 2D the decision engine and Jev, shadow mode first. **Final owner confirmation of SI-39's print clause (2026-09-22): APPROVED.** The last pending Phase 2C owner decision; every Phase 2C owner decision is now final. The exception documents existing behaviour and is not new authority: ordinary operator reads stay minimised; `npm run ops -- triage show --id …` is the one explicit owner-CLI exception, showing the stored proposal, reply draft included, of one specifically named review, as backend owner-CLI behaviour only. It grants the Phase 2C browser no access to the reply draft, does not make a review acceptance an approval of an outgoing message, and authorises no WhatsApp send.
- **PR #6 architecture review (2026-09-22; owner-directed docs fix before merge).** Reverses no owner decision. (1) The GitHub Codex P2 is resolved in the architecture: no browser projection returns a raw free-form actor label (the reviewer, `requested_by`, `tripped_by`, `cleared_by`, `marked_by` or `configured_by` value, or a principal's `display_name`); `ReviewSummary.reviewer`, `OutboundSummary.requestedBy`, `ExecutionStopSummary.trippedBy` and `clearedBy` and `OperatorContext.principal.displayName` are removed, no replacement identity field is added, and `EventSummary.source` passes only through a pinned provenance allowlist. (2) The overbroad "never any email" claim is replaced by a precise rule: the browser receives no identity or email field, while free-text content (decision notes, stop reasons, the advice summary and recommended next action) is content, not identity, and is not claimed free of email-like text; the proposed SI-56 is narrowly extended to say so. (3) Planned regression cases with email-like legacy labels in a reviewer, a stop actor and an outbound requester, and a note or reason that stays content. (4) `ALTER DEFAULT PRIVILEGES` precision, so that no default privilege gives `ops_operator_api`, `authenticated`, `anon` or PUBLIC future authority over `company_os_api` or `ops`: three mechanical tests, each a non-overridable finding in every migration — any form (GRANT or REVOKE) naming `company_os_api` or `ops_operator_api`, any GRANT form with no `IN SCHEMA` clause, and any GRANT form with `ops` in its `IN SCHEMA` list — plus a live pin of the `pg_default_acl` entries that reach those schemas. (5) Status and wording: all four amended SI texts are labelled owner-approved final texts; PR #6 is open and not merged; the Reviews screen is the Reviews / Decision Queue; the accept confirmation's required semantics are pinned (it records the structured review decision only, does not approve the reply draft and sends nothing); the implementation drift list names the existing "approved reply" wording; the review derivation evidence cites the live `ops.open_review_for_settled_job` → `ops.open_review_for_run` path; Phase 2D assesses and proposes, and the owner decides; the acceleration review carries dated decision-P notes. See [PHASE_2C_BRIEF.md](PHASE_2C_BRIEF.md) §21 and [ADR 0019](adr/0019-company-os-operator-surface.md).
- **Phase 2C S0 closeout owner decisions S0-A to S0-H** (2026-09-22, [PHASE_2C_REPORT.md](PHASE_2C_REPORT.md) §2). Spikes S0.1 to S0.6 passed with no P0, P1 or material contradiction, and the owner authorised the read-only streams. **S0-A** APPROVED: the application shell may own the router boundary around the unedited `<CRM/>` mount, as a narrowly scoped wrapper in `src/App.tsx`, with cross-surface navigation tests; amends the "not wrapped in another router" wording of brief §0 item 1 and §6.2; not a reversal. **S0-B:** the browser trip gate's `lock_timeout` is 2 s; no lock-free pre-check. **S0-C:** local and synthetic harnesses may mint sessions by admin magic link; password login is not enabled for tests. **S0-D** (for S7): no owner-chosen initial password; a self email change uses GoTrue's confirmation flow and is never forced through admin `updateUserById`; an email never binds or transfers a Company OS principal. **S0-E:** the migration identity `postgres` is the trusted migration root and, as schema owner, can drop and recreate an exposed function after the OD-8a window; the static guard, exact allowlists, literal names, owner, ACL and `pg_default_acl` pins and the live-catalogue proof are the boundary, and the documentation no longer claims PostgreSQL prevents it. **S0-F:** never `GRANT <role> TO current_user`; the grantee is the literal asserted identity. **S0-G:** security tests recursively pin the login-role closure, every CREATEROLE role and every role able to become or grant `ops_operator_api`. **S0-H:** the build secret scanner keeps its exact names and adds class-based checks. S7 stays OPEN; the two browser mutations stay unbuilt.

Decisions A–E belong to ADR 0017's owner-review addendum; F–H settle Phase 1D.2; I–O settle the architecture acceleration review (N superseded for sequencing by P); P, OD-1 to OD-15, the final owner confirmation, the pre-push review and the PR #6 architecture review settle the Phase 2C owner review (ADR 0019). The acceleration review is the one whose §15 **OSS source-reuse policy the owner approved as written on 2026-09-17**. That policy governs approved implementation phases: cloning, downloading, running, modifying and forking an approved component's upstream repository is allowed after the §15.1 license gate, while cloning candidates merely to evaluate them during a research review is not. **Fork is not the default** — the mode follows the shape of the thing (package, external service behind an adapter, fork, source extraction, or reference and build), and the objective is the lowest total implementation, maintenance, upgrade and exit cost. The first real adoption creates `docs/OSS_PROVENANCE.md` with the fields listed in review §15.3; no placeholder file exists before then.

## How to use this

- Read the relevant ADR **before** changing anything it covers.
- To reverse one, write a new ADR that supersedes it. Do not edit the original except to set `Status: Superseded by NNNN` — or, as with 0002, to strike a claim that is demonstrably false, in place and dated.
- Anything structural — a new pattern, a new dependency, a deliberate departure from convention, a non-obvious schema choice — gets an ADR. Naming and file-layout micro-choices do not.

## Decisions deliberately NOT made yet

These are recorded so they are not settled by accident:

- **Where does engine code live?** No `apps/`/`packages/`/`services/` exists, so the `ra-core` boundary ADR 0005 requires to be *mechanical* is currently a convention (Q13). Options and a recommendation: [proposals/0001-repository-structure.md](proposals/0001-repository-structure.md). **No migration in Phase 0.5.**
- **Does the MCP function survive at all, and when is it removed or downgraded?** 0011 constrains it; it does not decide its fate. It must be settled **before `ops` exists**. *(2026-09-13: **not settled before `ops` existed.** `ops` was created in Phase 1A while the function kept its `postgres` pool and stayed in `deploy.yml`. ADR 0011 item 4 is undischarged, and it now blocks accepting ADR 0002.)* **Decided 2026-09-13 (owner):** it does not survive as a production channel. The function is removed, `scripts/production-scope.mjs` keeps it out of the committed deploy paths (SI-03), and the future interface is an explicit Tool Gateway with allowlisted operations.
- **Is inbound email (Postmark) in scope?** ~~The only untrusted external channel currently reaching the database.~~ *(2026-09-18: no longer the only one. Phase 2B adds the signed WhatsApp gateway, [ADR 0018](adr/0018-whatsapp-transport-and-human-send.md), Accepted with amendment 2026-09-21.)*
- **Documentation and UI language**, and where tenant vocabulary lives (`docs/project-context.json` does not exist yet).
- **Must the engine be emulated by the FakeRest provider?** This roughly doubles or halves the cost of every engine table.
- **Multi-tenant LGPD scope** — per-tenant legal basis, retention, DPA/DPIA, and whether the platform operator becomes a processor for its tenants. *(2026-09-13: still open. ADR 0015's owner addendum bears on it (businesses needing independent data isolation are separate tenants, and minimised event payloads), but per-tenant legal basis, retention, DPA/DPIA and the processor role remain undecided.)* **(2026-09-17, Phase 1D.1: an explicit BLOCKER for Phase 2A with REAL patient data.)** Since Phase 1D, task and agent text leaves the database for the model provider (`store: false` does not remove abuse-monitoring retention). It is not answered by assumption. Until the owner decides BASELINE Q8 — processor roles for each tenant, the provider's retention and zero-data-retention status, a DPA/DPIA, and the data classification of task text — Phase 2A may be built and tested with synthetic data only, and no real patient or clinical text may reach a model. **(2026-09-17, owner review: Q8 stays open.)** The owner confirmed that Q8 blocks real patient data only, and that synthetic Phase 2A development and testing are allowed. Until Q8 is resolved, no real patient message body, clinical text, psychotherapy information or health data may be sent to a real LLM provider.
- **How does a hosted tenant get its loss reasons?** *(added 2026-09-13, owner)* Loss reasons are tenant vocabulary (CLAUDE.md rule 2, ADR 0013), so no migration creates them. A hosted project has none until an explicit onboarding path provisions a tenant's own, and until then a hosted deal cannot be marked lost. Global reference data a production database needs, such as `favicons_excluded_domains`, ships in migrations (20260913120000).

~~Is the psychology pipeline tenant configuration or engine schema?~~ ✅ Decided — ADR 0013.
~~How does the worker obtain tenant context?~~ ✅ Decided — ADR 0012, accepted 2026-09-12: the tenant is resolved from a live lease.

See [BASELINE_REPORT.md §13](BASELINE_REPORT.md#13-open-questions) for the full list with evidence.
